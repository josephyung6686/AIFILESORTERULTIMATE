---
context: default
phase: P8-llm-harness-validator
task: 1
total_tasks: 12
status: in_progress
last_updated: 2026-08-25T09:35:59.781Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered in this session.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: explicit pathspec only — Never `git add -A`. P8 commits stage only `src/llm_harness/`, `tests/p8/`, and (later) `tests/integration/test_p8_p7_egress.py` / `tests/integration/test_p8_p2_replay.py`. A dirty or concurrently committed P1–P7/planning tree must not ride along.
- [ ] CONSTRAINT: Task 1 reviews incomplete — `dd94ee3` is implementation only. Spec compliance review and code quality review were interrupted. Do **not** start Task 2 until those reviews pass (or MUST-FIX gaps are committed).
- [ ] CONSTRAINT: do not close C-5 — P6 publishes neither `normalize(` nor `contradicts(`. P8 must not invent them. Site A uses injected `FactValidationDependencies` or returns `ValidationUnavailable`.
- [ ] CONSTRAINT: do not implement P9 — user asked to ship P8 only. P9 plan body still has stale names.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Staging the whole dirty tree | P8 Task 1 ran in a dirty P1–P7 worktree; `git add -A` would mix assembly, planning, and domain files into a P8 commit | blocking | Explicit pathspec in every P8 commit; verify `git show --stat` after commit |
| Skipping two-stage review | Controller was about to spec-review Task 1 when paused; jumping to Task 2 freezes an unreviewed record shape | blocking | Spec review then quality review of `dd94ee3` before Task 2 |
| Coercing NeedsConsent | Consent is not an outcome, reason code, event, or P2 row | blocking | Return P7's exact `NeedsConsent` unchanged; grep `needs_consent` out of OUTCOMES / ALL_REASON_CODES |
| Inventing P6 oracles | Closing C-5 in `facts/` or implementing `normalize(`/`contradicts(` in P8 | blocking | Inject callbacks; missing → `ValidationUnavailable` |
| Starting P9 | P9 plan not patched; user asked for P8 only | advisory | Implement only the P8 file map |

<current_state>

P8 ship paused mid-SDD on branch `build/p6-p7-first-packages`.

HEAD is `d7a0ea2` (`fix: connect and harden P1 through P7`). Immediately under it is **P8 Task 1**: `dd94ee3` (`feat(p8): define bounded harness contracts`).

Task 1 code exists: `src/llm_harness/{__init__,vocabulary,records}.py` and `tests/p8/{__init__,test_p8_records,test_p8_vocabulary}.py`. Implementer claimed 46 tests GREEN after RED collection (`ModuleNotFoundError: llm_harness`). Public `__all__` is `Dossier`, `P8Verdict`, `Refusal`, `ValidationUnavailable`, `NeedsConsent` (exact class from `privacy.release`). `run_call` is not stubbed.

Spec + quality review of Task 1 did **not** finish. Implementer status was `DONE_WITH_CONCERNS`:
- `Refusal` wraps live P7 `Denied` (not a positional reason-code constructor)
- `CallResult.value` is `P8Verdict | Refusal | PreCallAbstention | CallFailed` and rejects `NeedsConsent`
- Some disposition strings are snake_case derived from SPEC table prose

Do not treat those as approved until the spec reviewer says so.

</current_state>

<completed_work>

- Independent P1–P7 live re-verify (earlier this session): usable as intended. Report: `.superpowers/sdd/p1-p7-live-reverify.md` (untracked).
- P8 plan judged SHIP AS WRITTEN. Re-verify: `.superpowers/sdd/p8-plan-reverify.md` (untracked).
- P9 judged SHIP AFTER FIXES — **do not implement P9**.
- P8 Task 1 implementation committed: `dd94ee3`.
- Untracked notes: `.superpowers/sdd/p8-constraints.md`, `.superpowers/sdd/p8-task-1-brief.md`, `.superpowers/sdd/p8-task-1-report.md`.

</completed_work>

<remaining_work>

- Task 1: spec-review + quality-review `dd94ee3`; fix MUST-FIX if any; then mark complete.
- Task 2: Deterministic fingerprint and dossier identity (`src/llm_harness/fingerprint.py`, `tests/p8/test_p8_fingerprint.py`)
- Task 3: Append-only storage
- Task 4: Eligibility, suppression, budgets
- Task 5: Release-consuming transport
- Task 6: Universal validation
- Task 7: Site A fact validation
- Task 8: Sites B–E fixtures
- Task 9: Compose `Gate.release` branches / `run_call`
- Task 10: P2 stage output
- Task 11: No-bypass / no-invention guards
- Task 12: Walking skeleton

</remaining_work>

<decisions_made>

- Execute P8 with subagent-driven development (TDD, one task, spec then quality review, commit). Continuous — do not pause between tasks except BLOCKED or user stop (this pause is a user stop).
- Work in place on `build/p6-p7-first-packages`, not a new worktree, not `main`.
- Task 1 does not export `run_call`.
- Stdlib only in `src/llm_harness/`. May import live P1–P7 packages.
- P6 `Verdict` and P8 `P8Verdict` are different types. No bare `Verdict` export.

</decisions_made>

<blockers>
- None technical. Human paused to go offline.
- Codex may still be running in the existing terminal (domain/planning). Coordinate if it starts touching `src/llm_harness/` or `tests/p8/`.
</blockers>

## Required Reading (in order)

1. `planning/00-database-agent-product-design.md` — wins on conflict
1. `planning/parts/P8-llm-harness-validator/SPEC.md` — P8 contract
1. `planning/30-p8-p9-connection-contract.md` — frozen names/ownership
1. `planning/parts/P8-llm-harness-validator/PLAN.md` — executable plan (symlink to `docs/superpowers/plans/2026-08-25-p8-bounded-llm-harness-validator.md`)
1. `.superpowers/sdd/p8-constraints.md` and `.superpowers/sdd/p8-task-1-brief.md` if still untracked
1. Commit `dd94ee3` and `.superpowers/sdd/p8-task-1-report.md`

## Critical Anti-Patterns (do NOT repeat these)

- [ANTI-PATTERN]: `git add -A` on this branch → explicit pathspec; confirm with `git show --stat`
- [ANTI-PATTERN]: Start Task 2 before Task 1 two-stage review → reviews first
- [ANTI-PATTERN]: Close C-5 or invent `normalize(`/`contradicts(` → inject or `ValidationUnavailable`
- [ANTI-PATTERN]: Map `NeedsConsent` to abstain/deny/retry → return unchanged
- [ANTI-PATTERN]: Implement P9 or edit `src/production.py` / `src/orchestrator.py` / P6 / P7 / domains in a P8 task

## Infrastructure State

- Branch: `build/p6-p7-first-packages` (tracks origin; was ahead after P8 Task 1; Codex then committed P1–P7 as `d7a0ea2` on top)
- Python 3.12, `PYTHONPATH=src`, pytest from repo root
- No GSD `.planning/phases/` tree in this repo — handoff lives at `.planning/.continue-here.md`
- Codex terminal may still be active (do not assume it is idle)

## Pre-Execution Critique Required

_Not a spike. Skip. The next gate is Task 1 spec+quality review, not a new design._

<context>

User asked to ship P8. Controller loaded SDD, dispatched Task 1 implementer, got DONE_WITH_CONCERNS, started spec review, then the user said they are going offline. Do not continue implementing. Resume = reviews of Task 1, then remaining plan tasks 2–12.

</context>

<next_action>

Start with: independently spec-review commit `dd94ee3` against P8 Task 1 in `planning/parts/P8-llm-harness-validator/PLAN.md` (full task text). Then quality-review. Only after both pass, dispatch Task 2 implementer with the same SDD constraints.

To resume in Cursor: continue this thread or say “resume P8 ship”. GSD `/gsd:resume-work` will read `.planning/HANDOFF.json`.

</next_action>
