{
  "list_id": "camera_filename_patterns",
  "title": "04 — Camera and device filename patterns (P5 E5 `filename_pattern`)",
  "version": "1.0",
  "authored": "2026-08-20",
  "owner": "P5 E5 (`extract_image`), through the caller-supplied `filename_pattern` strategy — the list itself is injected, never resident in `src/extractors/`",
  "consumer": "`filename_pattern(filename) -> str | None`. A match is emitted as one observation at `zone = filename`, `label = None`, `reliability = possible`, **`signal_tier` absent**, `text_span` absent. P5 draws no conclusion from it; §2.6's photo/screenshot hierarchy is P6's.",
  "match_field": "the filename **stem** — `file_row[\"filename\"]` with its final extension removed. P5 passes the filename WITH extension, so stripping it is the adapter's first step. Stems only: `IMG_4821.png` and `IMG_4821.HEIC` are the same convention and must match the same row.",
  "normalization_for_matching": "Unicode NFC and leading/trailing whitespace stripped, **for comparison only**; P4 RAW-1/RAW-2 keep the stored value byte-for-byte. No case folding is applied to the value — case-insensitivity is per-row, and every row in v1 is case-insensitive because a filesystem, a card reader or an export step may have changed the case of a name a camera wrote in upper case.",
  "boundary_rule": "**Every pattern is anchored `^…$` against the whole stem.** There is no unanchored pattern and no substring match in this catalogue: a substring match on `IMG_` would fire on `IMG_of_the_lease`, and a substring match on the word *screenshot* would let E5 approximate a media-type verdict (see `ref-screenshot-word-anywhere`). Catalogue 01's boundary rule solves the same problem for prefix strings; here anchoring solves it outright.",
  "design_cites": [
    "§2.6 \"For every supported image, the engine should store its format, pixel dimensions, file size, colour information where useful, content hash, perceptual hash, EXIF camera make and model, lens data, ISO, focal length, capture time, GPS, orientation, software metadata, **filename pattern**, and OCR output where needed.\"",
    "§1.2/§2.1 \"`IMG_4821.png` may be a screenshot of a receipt, application portal, conversation, code error, or research figure\" — the design's own example, and its own statement that the name settles nothing",
    "§2.6 \"the system must not mistake the absence of EXIF for proof that an image is a screenshot. Messaging platforms and downloaded web images often strip metadata from real photographs. OCR text density is also not a reliable screenshot detector\" — the two traps this catalogue is written around",
    "P5 SPEC, *Deferred*: \"Camera-filename pattern library | §2.6 | That filename pattern is an emitted field; `IMG_4821.png` is the design's example | The pattern set\" — this file is that pattern set",
    "P5 PLAN Task 15: `matched = filename_pattern(file_row[\"filename\"])` … `emit(zone=\"filename\", raw=matched, label=None, reliability=\"possible\")` — the return value becomes `raw_value`",
    "P5 PLAN Task 20, the no-invention guard: \"No resolution, no ratio and no filename regex appears anywhere in P5, and Task 20 asserts it by introspection.\""
  ],
  "rules": [
    "**A pattern label names the naming convention, never the media type.** \"Apple/DCF camera-roll sequence\" and \"macOS screen-capture default filename\" name the software that wrote the name. No row says *photo*, *screenshot* or *scan*, because P5 SPEC E5 forbids E5 emitting a photo/screenshot conclusion and `media type` is a Photos-domain fact (§3.11) that belongs to P6.",
    "**`family` is a grouping of conventions, not a verdict.** It records which kind of software writes the name — `camera`, `messaging`, `screen capture` — so a reader can navigate the table. P5 never emits it, and a `screen capture` family value is not a screenshot classification: §2.6 requires a hierarchy of signals and abstention on conflict, and a filename is not even in §2.6's hierarchy.",
    "**No date is parsed, normalised, validated or emitted.** Several conventions embed a timestamp. Every such row recognises the *shape* (`\\d{8}_\\d{6}`) and stops there. §3.10's narrow date extraction is P6's, and `ref-bare-timestamp-shape` refuses the prefix-free form precisely so no reader is tempted.",
    "**Absence is never evidence.** No row fires on a filename having no pattern, and `filename_pattern` returning `None` writes no observation at all. §2.6's first trap.",
    "**Patterns are ordered and the first match wins.** Every pattern is anchored, so in v1 no two rows can match the same stem — `checks/check04.py` asserts that as an invariant, and asserts separately that `IMG_20240115_103045` resolves to the Android row and not to `cfp-dcf-img`.",
    "**Cited prefixes, not a generic wildcard.** The one pattern that would raise recall most — DCF's generic four-character basename — is refused, because `FALL2024` matches it. See `ref-generic-dcf-basename`.",
    "**P5 never holds this list.** It is injected as the `filename_pattern` strategy at construction. Nothing here may become a module-level constant inside `src/extractors/`; P5 PLAN Task 20 asserts by runtime introspection that no filename regex lives there."
  ],
  "injection": "P5's `extract_image` takes `filename_pattern` as a **required keyword with no default**. The caller builds the matcher from this JSON — `extract_image(..., filename_pattern=make_filename_matcher(load('04-camera-filename-patterns.json')))` — and the adapter is responsible for (a) stripping the extension, (b) NFC-normalising for comparison only, and (c) trying rows in file order and returning on the first match. The compiled patterns live in the adapter, never in `extractors`: Task 20 walks `vars(module)` for every module in `src/extractors/` and a resident regex fails it.",
  "coverage_note": "v1 covers the conventions for which a citable source was retrieved on 2026-08-20: DCF's six named prefixes, Nikon's two documented forms, AOSP's two camera format strings, macOS's screen-capture prefix, and WhatsApp's media-store form. It deliberately does **not** cover Canon's vendor prefixes, Pixel's `PXL_`, Android OEM screenshot forms, Windows' screenshot form, or any non-English macOS locale — each is in `uncertain` with the specific evidence that is missing. Recall is therefore known to be incomplete; that is the intended trade, because `filename_pattern` returning `None` costs one weak `possible` observation, while a wrong match puts a false convention name into the evidence record.",
  "sources": [
    {
      "title": "Design rule for Camera File system (DCF) — JEITA CP-3461",
      "url": "https://en.wikipedia.org/wiki/Design_rule_for_Camera_File_system",
      "retrieved": "2026-08-20",
      "note": "\"Design rule for Camera File system (DCF) is a JEITA specification (number CP-3461)\"; \"files with names such as 'ABCD1234.JPG' that consist of four alphanumeric characters (often '100_', 'DSC0', 'DSCF', 'IMG_', 'MOV_', or 'P000'), followed by a number in the range 0001...9999\"; \"One DCF directory may contain up to 9999 DCF objects, numbered from '0001' to '9999'\"; directories are \"names such as '123ABCDE'\". Basis for the six DCF prefix rows and for the refusal of the generic form."
    },
    {
      "title": "JEITA — standards listing for CP-3461 (Design rule for Camera File system)",
      "url": "https://www.jeita.or.jp/cgi-bin/standard_e/pdf.cgi?jk_n=51&jk_pdf_file=CP",
      "retrieved": "2026-08-20",
      "note": "The publisher's own listing for the standard number. Cited for provenance of CP-3461; the normative text itself is not free to read, which is why the naming rule above is quoted from the secondary source."
    },
    {
      "title": "Nikon D850 online manual — File naming",
      "url": "https://onlinemanual.nikonimglib.com/d850/en/18_menu_guide_03_04.html",
      "retrieved": "2026-08-20",
      "note": "\"Photographs are saved using file names consisting of 'DSC_' or, in the case of images that use the Adobe RGB color space, '_DSC', followed by a four-digit number and a three-letter extension (e.g., 'DSC_0001.JPG').\" The same page notes the three letters are user-configurable — the recall argument recorded on `ref-generic-dcf-basename`."
    },
    {
      "title": "AOSP — Camera: Clean up the image/video file naming code (aa61079c7dafc5f2b8a99415551a2aa8e7ac6ad7)",
      "url": "https://android.googlesource.com/platform/packages/apps/Camera/+/aa61079%5E%21/",
      "retrieved": "2026-08-20",
      "note": "The commit that introduced the format strings `\"'IMG'_yyyyMMdd_HHmmss\"` and `\"'VID'_yyyyMMdd_HHmmss\"` (examples IMG_20091020_120000.jpg, VID_20091020_120000.3gp). Primary source for the two Android camera rows."
    },
    {
      "title": "Apple — Take screenshots or screen recordings on Mac",
      "url": "https://support.apple.com/guide/mac-help/take-screenshots-or-screen-recordings-mh26782/mac",
      "retrieved": "2026-08-20",
      "note": "\"Screenshots are saved as .png files and screen recordings are saved as .mov files. Filenames begin with 'Screenshot' or 'Screen Recording' and include the date and time.\" Apple documents the prefix and the presence of a date/time, not the layout — so the date/time half of `cfp-macos-screencapture` is observed, not cited, and is flagged as locale-shaped."
    },
    {
      "title": "Forensic Focus — WhatsApp media file naming convention (forum thread)",
      "url": "https://www.forensicfocus.com/forums/general/whatsapp-media-file-naming-convention/",
      "retrieved": "2026-08-20",
      "note": "Forensic-practitioner description of the received-media form `IMG-YYYYMMDD-WAnnnn`. WhatsApp publishes no naming specification, so this and the thesis below are the best available sourcing; both are secondary. The pattern's shape is distinctive enough that the false-positive risk does not depend on the source's strength."
    },
    {
      "title": "Risemberg — File Structure Analysis of Media Files Sent and Received Over WhatsApp (Univ. of Colorado Denver, NCMF, Spring 2020)",
      "url": "https://www.ucdenver.edu/docs/librariesprovider27/ncmf-docs/theses/risemberg_thesis_spring2020.pdf",
      "retrieved": "2026-08-20",
      "note": "Located on 2026-08-20 as academic corroboration of the same `IMG-YYYYMMDD-WAnnnn` / `VID-YYYYMMDD-WAnnnn` convention. **The PDF itself was not opened**, so it is cited as corroborating literature only, not quoted."
    }
  ],
  "entries": [
    {
      "id": "cfp-android-camera-still",
      "pattern": "^IMG_\\d{8}_\\d{6}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "capture": 0,
      "pattern_label": "Android camera date-stamped still",
      "family": "camera",
      "rationale": "The AOSP camera application's still-image format string. The eight digits are a calendar date and the six a wall-clock time, but this row recognises the SHAPE only: nothing here parses, normalises, validates or emits a date — §3.10's narrow date extraction is P6's and explicitly refuses fuzzy parsing. Listed first so it wins over `cfp-dcf-img`; both are anchored, so the ordering is belt-and-braces.",
      "design_cite": "§2.6 \"For every supported image, the engine should store its format, pixel dimensions, file size, colour information where useful, content hash, perceptual hash, EXIF camera make and model, lens data, ISO, focal length, capture time, GPS, orientation, software metadata, **filename pattern**, and OCR output where needed.\"",
      "false_positive_risk": "very low — the fixed `IMG_` + 8 + `_` + 6 shape is not a form personal filenames take",
      "example_true": "IMG_20240115_103045",
      "example_false": "IMG_2024",
      "source": "AOSP commit aa61079c7dafc5f2b8a99415551a2aa8e7ac6ad7, *Camera: Clean up the image/video file naming code* — image format string `\"'IMG'_yyyyMMdd_HHmmss\"`, video format string `\"'VID'_yyyyMMdd_HHmmss\"`"
    },
    {
      "id": "cfp-android-camera-video",
      "pattern": "^VID_\\d{8}_\\d{6}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "capture": 0,
      "pattern_label": "Android camera date-stamped video",
      "family": "camera",
      "rationale": "The video half of the same AOSP format pair. Catalogued for completeness of the convention; E5 is only ever handed images, so this row will normally not fire. Same shape-only rule: no date is parsed.",
      "design_cite": "§2.6 \"For every supported image, the engine should store its format, pixel dimensions, file size, colour information where useful, content hash, perceptual hash, EXIF camera make and model, lens data, ISO, focal length, capture time, GPS, orientation, software metadata, **filename pattern**, and OCR output where needed.\"",
      "false_positive_risk": "very low",
      "example_true": "VID_20240115_103045",
      "example_false": "VID_2024",
      "source": "AOSP commit aa61079c7dafc5f2b8a99415551a2aa8e7ac6ad7, *Camera: Clean up the image/video file naming code* — image format string `\"'IMG'_yyyyMMdd_HHmmss\"`, video format string `\"'VID'_yyyyMMdd_HHmmss\"`"
    },
    {
      "id": "cfp-whatsapp-image",
      "pattern": "^IMG-\\d{8}-WA\\d{4}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "capture": 0,
      "pattern_label": "WhatsApp media-store naming (image)",
      "family": "messaging",
      "rationale": "The messaging class §2.6 needs. §2.6's first trap is that \"Messaging platforms and downloaded web images often strip metadata from real photographs\" — so a stripped-EXIF image is NOT evidence of a screenshot. This row is the positive form of that sentence: a name in WhatsApp's media-store shape is evidence that a messaging platform handled the file, which is what P6 needs in order to explain missing EXIF from presence rather than from absence. Hyphens, not underscores, so it cannot collide with the Android camera rows.",
      "design_cite": "§2.6 \"the system must not mistake the absence of EXIF for proof that an image is a screenshot. Messaging platforms and downloaded web images often strip metadata from real photographs.\"",
      "false_positive_risk": "very low — the literal `-WA` infix between a date shape and a four-digit counter has no personal-filename analogue",
      "example_true": "IMG-20240115-WA0001",
      "example_false": "IMG-20240115-0001",
      "source": "Risemberg, *File Structure Analysis of Media Files Sent and Received Over WhatsApp* (Univ. of Colorado Denver, NCMF, Spring 2020) and the Forensic Focus thread *WhatsApp media file naming convention* — both describe the received-media form `IMG-YYYYMMDD-WAnnnn.jpg` / `VID-YYYYMMDD-WAnnnn.mp4`. WhatsApp publishes no naming specification."
    },
    {
      "id": "cfp-whatsapp-video",
      "pattern": "^VID-\\d{8}-WA\\d{4}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "capture": 0,
      "pattern_label": "WhatsApp media-store naming (video)",
      "family": "messaging",
      "rationale": "The video counterpart, catalogued so the convention is complete and so a future non-image consumer does not have to re-derive it.",
      "design_cite": "§2.6, as above — the same messaging-platform explanation for stripped metadata",
      "false_positive_risk": "very low",
      "example_true": "VID-20240115-WA0001",
      "example_false": "VID-20240115-0001",
      "source": "Risemberg, *File Structure Analysis of Media Files Sent and Received Over WhatsApp* (Univ. of Colorado Denver, NCMF, Spring 2020) and the Forensic Focus thread *WhatsApp media file naming convention* — both describe the received-media form `IMG-YYYYMMDD-WAnnnn.jpg` / `VID-YYYYMMDD-WAnnnn.mp4`. WhatsApp publishes no naming specification."
    },
    {
      "id": "cfp-macos-screencapture",
      "pattern": "^Screen ?Shot \\d{4}-\\d{2}-\\d{2} at \\d{1,2}[.:]\\d{2}[.:]\\d{2}(\\s*[AP]M)?$",
      "match_kind": "regex",
      "case_sensitive": false,
      "capture": 0,
      "pattern_label": "macOS screen-capture default filename",
      "family": "screen capture",
      "rationale": "Apple documents the PREFIX and that the name carries a date and time; it does not publish the layout, so the date/time part of this pattern is an observed en-US form, not a cited one. `Screen ?Shot` covers both spellings (macOS wrote `Screen Shot` before Monterey and `Screenshot` after). Case-insensitive. The trailing `\\s*` absorbs the narrow no-break space (U+202F) that recent macOS versions place before AM/PM. **The label names the tool that wrote the name, never a media type**: a match is not a screenshot verdict, and §2.6 gives filenames no place in its signal hierarchy at all.",
      "design_cite": "§2.6 \"exact display resolutions, PNG format, and software metadata may support a screenshot hypothesis\" — *may support*, and a filename is not even on that list; the verdict is P6's",
      "false_positive_risk": "low for en-US; the pattern is locale-shaped and misses localised names — a recall gap, recorded in `uncertain`",
      "example_true": "Screenshot 2026-08-20 at 10.30.45 AM",
      "example_false": "Screenshot of my lease",
      "source": "Apple, *Take screenshots or screen recordings on Mac*: \"Screenshots are saved as .png files and screen recordings are saved as .mov files. Filenames begin with \\\"Screenshot\\\" or \\\"Screen Recording\\\" and include the date and time.\""
    },
    {
      "id": "cfp-nikon-dsc-srgb",
      "pattern": "^DSC_\\d{4}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "capture": 0,
      "pattern_label": "Nikon in-camera file name (sRGB)",
      "family": "camera",
      "rationale": "Nikon's own manual gives this form verbatim. Note the same manual says the three letters are user-configurable — `File naming` lets the photographer \"select three letters to replace the 'DSC' portion\" — which is precisely why this catalogue lists cited prefixes rather than a generic four-character wildcard (see `ref-generic-dcf-basename`).",
      "design_cite": "§2.6 \"For every supported image, the engine should store its format, pixel dimensions, file size, colour information where useful, content hash, perceptual hash, EXIF camera make and model, lens data, ISO, focal length, capture time, GPS, orientation, software metadata, **filename pattern**, and OCR output where needed.\"",
      "false_positive_risk": "low — `DSC_` plus exactly four digits and nothing else",
      "example_true": "DSC_0001",
      "example_false": "DSC_00001",
      "source": "Nikon D850 online manual, *File naming*: \"Photographs are saved using file names consisting of 'DSC_' or, in the case of images that use the Adobe RGB color space, '_DSC', followed by a four-digit number and a three-letter extension (e.g., 'DSC_0001.JPG').\""
    },
    {
      "id": "cfp-nikon-dsc-adobergb",
      "pattern": "^_DSC\\d{4}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "capture": 0,
      "pattern_label": "Nikon in-camera file name (Adobe RGB)",
      "family": "camera",
      "rationale": "The Adobe RGB variant, from the same sentence of the same manual: the underscore moves to the front. A leading underscore also makes this shape essentially impossible as a hand-typed personal filename.",
      "design_cite": "§2.6 \"For every supported image, the engine should store its format, pixel dimensions, file size, colour information where useful, content hash, perceptual hash, EXIF camera make and model, lens data, ISO, focal length, capture time, GPS, orientation, software metadata, **filename pattern**, and OCR output where needed.\"",
      "false_positive_risk": "very low",
      "example_true": "_DSC0001",
      "example_false": "_DSC001",
      "source": "Nikon D850 online manual, *File naming*: \"Photographs are saved using file names consisting of 'DSC_' or, in the case of images that use the Adobe RGB color space, '_DSC', followed by a four-digit number and a three-letter extension (e.g., 'DSC_0001.JPG').\""
    },
    {
      "id": "cfp-dcf-img",
      "pattern": "^IMG_\\d{4}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "capture": 0,
      "pattern_label": "DCF camera-roll sequence, `IMG_` prefix",
      "family": "camera",
      "rationale": "§1.2's own example, `IMG_4821.png`. `IMG_` is one of the four-character prefixes DCF names, and the four digits are DCF's 0001–9999 object number. **This row records the naming convention, not the media type.** The design's whole point in naming this file is that it settles nothing: on iOS a screenshot saved to Photos is renamed into the same camera-roll sequence, so an `IMG_####` match is fully consistent with a screenshot, a photo, or a saved web image. P5 emits it at `reliability: possible` with `signal_tier` absent, and P6 decides — or abstains.",
      "design_cite": "§1.2/§2.1 \"`IMG_4821.png` may be a screenshot of a receipt, application portal, conversation, code error, or research figure\" — the design's own example, and its own statement that the name settles nothing",
      "false_positive_risk": "low as a CONVENTION match; the risk that matters here is misreading it as a photo verdict, which the label and the absent `signal_tier` both refuse",
      "example_true": "IMG_4821",
      "example_false": "IMG_20240115_103045",
      "source": "JEITA CP-3461 (DCF), via Wikipedia: \"files with names such as 'ABCD1234.JPG' that consist of four alphanumeric characters (often '100_', 'DSC0', 'DSCF', 'IMG_', 'MOV_', or 'P000'), followed by a number in the range 0001...9999\""
    },
    {
      "id": "cfp-dcf-mov",
      "pattern": "^MOV_\\d{4}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "capture": 0,
      "pattern_label": "DCF camera-roll sequence, `MOV_` prefix",
      "family": "camera",
      "rationale": "DCF's video counterpart to `IMG_`, named in the same source sentence. Included so the convention is complete.",
      "design_cite": "§2.6 \"For every supported image, the engine should store its format, pixel dimensions, file size, colour information where useful, content hash, perceptual hash, EXIF camera make and model, lens data, ISO, focal length, capture time, GPS, orientation, software metadata, **filename pattern**, and OCR output where needed.\"",
      "false_positive_risk": "very low",
      "example_true": "MOV_4821",
      "example_false": "MOV_482",
      "source": "JEITA CP-3461 (DCF), via Wikipedia: \"files with names such as 'ABCD1234.JPG' that consist of four alphanumeric characters (often '100_', 'DSC0', 'DSCF', 'IMG_', 'MOV_', or 'P000'), followed by a number in the range 0001...9999\""
    },
    {
      "id": "cfp-dcf-dscf",
      "pattern": "^DSCF\\d{4}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "capture": 0,
      "pattern_label": "DCF camera file name, `DSCF` prefix",
      "family": "camera",
      "rationale": "One of the six prefixes DCF's source sentence lists. Commonly a Fujifilm body, but the source attributes no vendor and neither does this row — the catalogued fact is the prefix, not the manufacturer.",
      "design_cite": "§2.6 \"For every supported image, the engine should store its format, pixel dimensions, file size, colour information where useful, content hash, perceptual hash, EXIF camera make and model, lens data, ISO, focal length, capture time, GPS, orientation, software metadata, **filename pattern**, and OCR output where needed.\"",
      "false_positive_risk": "very low",
      "example_true": "DSCF1234",
      "example_false": "DSCF12345",
      "source": "JEITA CP-3461 (DCF), via Wikipedia: \"files with names such as 'ABCD1234.JPG' that consist of four alphanumeric characters (often '100_', 'DSC0', 'DSCF', 'IMG_', 'MOV_', or 'P000'), followed by a number in the range 0001...9999\""
    },
    {
      "id": "cfp-dcf-dsc0",
      "pattern": "^DSC0\\d{4}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "capture": 0,
      "pattern_label": "DCF camera file name, `DSC0` prefix",
      "family": "camera",
      "rationale": "A second prefix from the same sentence; it produces the familiar `DSC00001` form. Distinct from `cfp-nikon-dsc-srgb`, which has an underscore in the fourth position instead of a zero.",
      "design_cite": "§2.6 \"For every supported image, the engine should store its format, pixel dimensions, file size, colour information where useful, content hash, perceptual hash, EXIF camera make and model, lens data, ISO, focal length, capture time, GPS, orientation, software metadata, **filename pattern**, and OCR output where needed.\"",
      "false_positive_risk": "very low",
      "example_true": "DSC00001",
      "example_false": "DSC0001",
      "source": "JEITA CP-3461 (DCF), via Wikipedia: \"files with names such as 'ABCD1234.JPG' that consist of four alphanumeric characters (often '100_', 'DSC0', 'DSCF', 'IMG_', 'MOV_', or 'P000'), followed by a number in the range 0001...9999\""
    },
    {
      "id": "cfp-dcf-p000",
      "pattern": "^P000\\d{4}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "capture": 0,
      "pattern_label": "DCF camera file name, `P000` prefix",
      "family": "camera",
      "rationale": "The literal `P000` prefix from the source sentence. Deliberately literal: the broader `P` + seven digits form some bodies use is not in the source and sits in `uncertain`.",
      "design_cite": "§2.6 \"For every supported image, the engine should store its format, pixel dimensions, file size, colour information where useful, content hash, perceptual hash, EXIF camera make and model, lens data, ISO, focal length, capture time, GPS, orientation, software metadata, **filename pattern**, and OCR output where needed.\"",
      "false_positive_risk": "very low",
      "example_true": "P0001234",
      "example_false": "P1010001",
      "source": "JEITA CP-3461 (DCF), via Wikipedia: \"files with names such as 'ABCD1234.JPG' that consist of four alphanumeric characters (often '100_', 'DSC0', 'DSCF', 'IMG_', 'MOV_', or 'P000'), followed by a number in the range 0001...9999\""
    },
    {
      "id": "cfp-dcf-100u",
      "pattern": "^100_\\d{4}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "capture": 0,
      "pattern_label": "DCF camera file name, `100_` prefix",
      "family": "camera",
      "rationale": "The last of the six prefixes the source sentence lists. `100` is also DCF's first directory number, which is why bodies that reuse the directory number as the file prefix produce this shape.",
      "design_cite": "§2.6 \"For every supported image, the engine should store its format, pixel dimensions, file size, colour information where useful, content hash, perceptual hash, EXIF camera make and model, lens data, ISO, focal length, capture time, GPS, orientation, software metadata, **filename pattern**, and OCR output where needed.\"",
      "false_positive_risk": "very low",
      "example_true": "100_0001",
      "example_false": "100_00012",
      "source": "JEITA CP-3461 (DCF), via Wikipedia: \"files with names such as 'ABCD1234.JPG' that consist of four alphanumeric characters (often '100_', 'DSC0', 'DSCF', 'IMG_', 'MOV_', or 'P000'), followed by a number in the range 0001...9999\""
    }
  ],
  "refused": [
    {
      "id": "ref-generic-dcf-basename",
      "pattern": "^[A-Za-z0-9_]{4}\\d{4}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "family": "camera",
      "pattern_label": "— (refused)",
      "rationale": "The fully generic DCF basename — any four alphanumerics plus a four-digit number — is the single highest-recall pattern available and is **refused**, because eight characters of that shape are an ordinary personal filename: `FALL2024`, `MATH1234`, `HIST2020`, `NOTE2024`, `PLAN2026` all match. E5 only ever sees images, but `FALL2024.png` is not a strange file to own. This is the same discipline catalogue 01 applies to bare vendor names: prefer a set of cited, discriminating prefixes over one loose form. Nikon's manual makes the recall cost explicit — the three-letter prefix is user-configurable, so a renamed prefix is genuinely missed. That trade is Joseph's to overturn, not this catalogue's.",
      "design_cite": "§2.6 \"filenames often carry little semantic meaning\" — which cuts both ways: a weak signal is not worth a false one",
      "false_positive_risk": "would be high",
      "example_true": "—",
      "example_false": "FALL2024",
      "source": "JEITA CP-3461 (DCF), via Wikipedia: \"files with names such as 'ABCD1234.JPG' that consist of four alphanumeric characters (often '100_', 'DSC0', 'DSCF', 'IMG_', 'MOV_', or 'P000'), followed by a number in the range 0001...9999\""
    },
    {
      "id": "ref-bare-sequence-number",
      "pattern": "^\\d{4}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "family": "camera",
      "pattern_label": "— (refused)",
      "rationale": "A bare four-digit stem. Some cameras and most export pipelines drop the prefix, so `4821.jpg` is a real camera output — and it is also a year, a page number, a ticket number, a course number and a scan sequence. No prefix, no evidence.",
      "design_cite": "§3.10 \"file names and documents frequently contain numbers that look like years but are course identifiers, version numbers, build numbers, ZIP codes, or other unrelated values\"",
      "false_positive_risk": "would be very high",
      "example_true": "—",
      "example_false": "2024"
    },
    {
      "id": "ref-bare-timestamp-shape",
      "pattern": "^\\d{8}_\\d{6}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "family": "camera",
      "pattern_label": "— (refused)",
      "rationale": "A prefix-free timestamp stem. Refused twice over: it is written by screen recorders, download managers, log rotators and export scripts as well as cameras, and matching it invites the next reader to parse it. **No entry in this catalogue parses a date.** §3.10's narrow date extraction is P6's, and this catalogue recognises shapes only.",
      "design_cite": "§3.10 \"Date extraction should be deliberately narrow. The product must not use fuzzy date parsing\" — and it is P6's, not P5's",
      "false_positive_risk": "would be high",
      "example_true": "—",
      "example_false": "20240115_103045"
    },
    {
      "id": "ref-screenshot-word-anywhere",
      "pattern": "screenshot",
      "match_kind": "regex",
      "case_sensitive": false,
      "family": "screen capture",
      "pattern_label": "— (refused)",
      "rationale": "Matching the word *screenshot* anywhere in a stem. Refused because it is a **user's** word, not a tool's: `screenshot of my lease.png` is a person describing a file, and the catalogue's job is to recognise names that software wrote. Refused a second time because a substring match on that word would let E5 approximate the media-type verdict §2.6 reserves for a hierarchy of signals and P5 SPEC E5 forbids E5 from reaching. `cfp-macos-screencapture` is anchored end to end for exactly this reason.",
      "design_cite": "§2.6 \"conflicting signals should lead to abstention rather than an invented classification\"",
      "false_positive_risk": "would be high",
      "example_true": "—",
      "example_false": "screenshot of my lease"
    }
  ],
  "uncertain": [
    {
      "id": "unc-return-value-contract",
      "pattern": "—",
      "match_kind": "regex",
      "case_sensitive": false,
      "pattern_label": "—",
      "family": "—",
      "rationale": "**Open contract question, for Joseph.** Does `filename_pattern(filename) -> str | None` return the matched substring or the name of the pattern? P5 PLAN Task 15 does `emit(zone=\"filename\", raw=matched, label=None, reliability=\"possible\")`, so whatever it returns becomes `raw_value` — and its own test asserts `raw_value == \"IMG_4821\"`, i.e. the substring. But `label=None` means the convention's NAME is then stored nowhere, and the convention is the part a human can act on. Every entry here carries both `capture` (which group is the substring; always 0, the whole stem-match) and `pattern_label`, so the adapter can satisfy either reading without a re-write. Recommendation, not a decision: return the substring, matching the existing test, and let P6 re-derive the label by re-running this list.",
      "design_cite": "P5 PLAN Task 15 `test_the_filename_pattern_is_the_callers_match` — `assert pattern[\"raw_value\"] == \"IMG_4821\"` and `assert pattern[\"signal_tier\"] is None`",
      "false_positive_risk": "n/a",
      "example_true": "—",
      "example_false": "—"
    },
    {
      "id": "unc-canon-prefixes",
      "pattern": "^(_MG_|MVI_|CRW_)\\d{4}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "pattern_label": "Canon in-camera file name",
      "family": "camera",
      "rationale": "Canon's `_MG_` (Adobe RGB still), `MVI_` (video) and `CRW_` (raw) prefixes are widely reported and would be a clean, low-risk addition. **Omitted because the source could not be read**: Canon's own KB article (ART156893) is a JavaScript-rendered page that returned only a loading shell on 2026-08-20, and this catalogue does not cite a page it has not read. Needs one Canon KB page retrieved by hand, or one real Canon card inspected.",
      "design_cite": "§2.6 \"For every supported image, the engine should store its format, pixel dimensions, file size, colour information where useful, content hash, perceptual hash, EXIF camera make and model, lens data, ISO, focal length, capture time, GPS, orientation, software metadata, **filename pattern**, and OCR output where needed.\"",
      "false_positive_risk": "low if real",
      "example_true": "_MG_1234",
      "example_false": "_MG_ImageStore"
    },
    {
      "id": "unc-pixel-pxl",
      "pattern": "^PXL_\\d{8}_\\d{6}\\d{0,3}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "pattern_label": "Google Pixel camera capture",
      "family": "camera",
      "rationale": "Pixel devices write `PXL_20240115_103045123.jpg` — the AOSP shape with a `PXL_` prefix and millisecond digits. No primary source (AOSP commit, Google support page, or vendor documentation) was found on 2026-08-20; the AOSP commit that IS cited predates the Pixel camera app and specifies `IMG_`. Distinctive enough to be near-zero risk, so this is a sourcing gap rather than a design doubt.",
      "design_cite": "§2.6 \"For every supported image, the engine should store its format, pixel dimensions, file size, colour information where useful, content hash, perceptual hash, EXIF camera make and model, lens data, ISO, focal length, capture time, GPS, orientation, software metadata, **filename pattern**, and OCR output where needed.\"",
      "false_positive_risk": "very low if real",
      "example_true": "PXL_20240115_103045123",
      "example_false": "PXL_2024"
    },
    {
      "id": "unc-android-screenshot",
      "pattern": "^Screenshot_\\d{8}[-_]\\d{6}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "pattern_label": "Android screen-capture default filename",
      "family": "screen capture",
      "rationale": "Android screen captures are commonly `Screenshot_20240115-103045.png`, but the separator and the presence of an app-name suffix vary by OEM skin (Samsung One UI, Xiaomi MIUI and stock AOSP differ) and no primary source was found on 2026-08-20. Adding a pattern whose separator varies by vendor without a source risks catching some devices and silently missing others, which is worse than a clean gap.",
      "design_cite": "§2.6 \"For every supported image, the engine should store its format, pixel dimensions, file size, colour information where useful, content hash, perceptual hash, EXIF camera make and model, lens data, ISO, focal length, capture time, GPS, orientation, software metadata, **filename pattern**, and OCR output where needed.\"",
      "false_positive_risk": "low if real",
      "example_true": "Screenshot_20240115-103045",
      "example_false": "Screenshot_notes"
    },
    {
      "id": "unc-windows-screenshot",
      "pattern": "^Screenshot \\(\\d+\\)$",
      "match_kind": "regex",
      "case_sensitive": false,
      "pattern_label": "Windows screen-capture default filename",
      "family": "screen capture",
      "rationale": "Windows' Win+PrtScn writes `Screenshot (12).png` into Pictures\\Screenshots. No Microsoft documentation of the filename form was retrieved on 2026-08-20. Note that ` (12)` is also the shape Windows and macOS both use for a **duplicate-name suffix**, so this pattern is entangled with a second, unrelated convention — worth settling deliberately rather than by reflex.",
      "design_cite": "§2.6 \"For every supported image, the engine should store its format, pixel dimensions, file size, colour information where useful, content hash, perceptual hash, EXIF camera make and model, lens data, ISO, focal length, capture time, GPS, orientation, software metadata, **filename pattern**, and OCR output where needed.\"",
      "false_positive_risk": "low if real",
      "example_true": "Screenshot (12)",
      "example_false": "Screenshot (final)"
    },
    {
      "id": "unc-apple-edited-img-e",
      "pattern": "^IMG_E\\d{4}$",
      "match_kind": "regex",
      "case_sensitive": false,
      "pattern_label": "Apple edited-copy camera-roll sequence",
      "family": "camera",
      "rationale": "iOS writes `IMG_E4821.JPG` beside `IMG_4821.JPG` when a photo has been edited in Photos. If real it is useful — it names an edited derivative, which is version-family evidence — but Apple publishes no statement of it and none was found on 2026-08-20. Its `E` also makes it a near-miss for `cfp-dcf-img`, so it must be ordered before that row if it is ever promoted.",
      "design_cite": "§1.2/§2.1 \"`IMG_4821.png` may be a screenshot of a receipt, application portal, conversation, code error, or research figure\" — the design's own example, and its own statement that the name settles nothing",
      "false_positive_risk": "very low if real",
      "example_true": "IMG_E4821",
      "example_false": "IMG_Evening"
    },
    {
      "id": "unc-macos-locale-forms",
      "pattern": "—",
      "match_kind": "regex",
      "case_sensitive": false,
      "pattern_label": "macOS screen-capture filename, non-English locales",
      "family": "screen capture",
      "rationale": "`cfp-macos-screencapture` is an en-US pattern: the literal word `at`, the `AM`/`PM` suffix and the `YYYY-MM-DD` order are all locale-dependent, and a German Mac writes `Bildschirmfoto 2026-08-20 um 10.30.45.png`. This is a **recall** gap, not a false-positive one — the catalogue simply returns `None` — and closing it needs a decision about which locales the product supports before it needs patterns. §2.7 already flags the same question for OCR languages.",
      "design_cite": "§2.7 \"appropriate language support including CJK where required\" — the same unanswered locale question, one stage earlier",
      "false_positive_risk": "n/a — a recall gap",
      "example_true": "Bildschirmfoto 2026-08-20 um 10.30.45",
      "example_false": "Bildschirmfoto"
    },
    {
      "id": "unc-duplicate-and-burst-suffixes",
      "pattern": "^IMG_\\d{4}[ ]?\\(\\d+\\)$",
      "match_kind": "regex",
      "case_sensitive": false,
      "pattern_label": "camera-roll name with a copy suffix",
      "family": "camera",
      "rationale": "`IMG_4821 (1).jpg`, `IMG_4821-2.jpg` and `IMG_4821 copy.jpg` are the same camera-roll name after a copy, an AirDrop or a re-download. Recognising them would feed §6's duplicate/version-family work directly — but the suffix vocabulary is filesystem- and locale-dependent (`copy`, `Kopie`, ` (1)`, `-2`), and duplicate detection has hashes, which §2.6 says are the reliable instrument. Deferred as a question for whoever owns version families, not for this list.",
      "design_cite": "§2.6 \"Exact hashes and perceptual hashes can identify duplicates and near-duplicates\" — the better instrument already exists",
      "false_positive_risk": "low if real",
      "example_true": "IMG_4821 (1)",
      "example_false": "IMG_4821"
    }
  ]
}
