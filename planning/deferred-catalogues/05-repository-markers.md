# 05 — Repository markers and package manifests (two arrays, two owners)

- **list_id**: repository_markers
- **version**: 1.0
- **authored**: 2026-08-20
- **owner**: `p3_exclusion_roots` → P3 · `p5_evidence_markers` → P5 (injected into the caller's reader)
- **consumer**: `p3_exclusion_roots` → P3's R3 exclusion verdict, which skips **descendants** of the marker-bearing directory. `p5_evidence_markers` → the `StructuralMarker(kind, value)` tuples that a caller-supplied `read_text_document` returns, and the `recognize_markers` recognizer E4 uses on archive members.
- **match_field**: a filename, a directory name, or a top-level notebook JSON key — each row says which in `applies_to`. `StructuralMarker.value` stores the marker "verbatim" (P5 PLAN Task 12), so the recorded value is the spelling found on disk, not a normalized one.
- **normalization for matching**: Exact string comparison. Case-insensitive by default because macOS and Windows filesystems are case-insensitive; the rows whose bare word is ordinary English (`LICENSE`, `COPYING`, `WORKSPACE`, `DESCRIPTION`, `README`, `CODEOWNERS`) are case-**sensitive** so that a document named `licence` or `description` cannot match.
- **sourcing**: **How to read the `verification` column, because it is not decoration.** Only five pages were opened and read directly while authoring these catalogues; three more were opened by the catalogues-finish agent, who supplied verbatim quotes. Everything else is marked `search_summary`: the fact came from a web-search result summary and the page itself was not retrieved. Those rows say `consulted` rather than `retrieved`, because claiming a retrieval date for a page nobody opened is exactly the kind of unearned confidence these catalogues exist to keep out of the product. A `search_summary` source is not worthless — it is how most of the vendor-prefix and manifest-name knowledge here was gathered — but it must not be quoted as if read, and a row that would be harmful if wrong should be upgraded before launch.

## Design basis

- §1.1 "It should also reject descendants of software project roots indicated by files such as `package.json`, `requirements.txt`, `Cargo.toml`, or `go.mod`. This prevents the proposal engine from mistaking a dependency subdirectory for a meaningful personal destination."
- §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files"
- §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project."
- §2.4: "Code-related files should rely heavily on local structural evidence, including repository roots and package files, rather than forcing semantic analysis to infer a project from arbitrary code text."
- P5 SPEC Deferred: "Repository markers and package manifests beyond §1.1's four | §2.4, §1.1, §2.5 | `package.json`, `requirements.txt`, `Cargo.toml`, `go.mod` (§1.1); `README.md`, `package.json`, `src`, Python package layout (§2.5) | Everything else; §1.1's list is P3's"
- P3 SPEC Deferred: "Software-project-root markers beyond §1.1's four … The four literal names are implementable now; any extension is hand-authored."
- P5 PLAN Task 12: `STRUCTURAL_MARKER_KINDS = ("repository marker", "package manifest", "notebook metadata", "README file")` — a fifth class raises `UnknownMarkerKind`.

## Rules this list obeys

1. **Two arrays, because there are two jobs.** `p3_exclusion_roots` answers *should this whole subtree be skipped before scanning?* `p5_evidence_markers` answers *does this admitted file look like part of a project?* A name may be in both — `package.json` is — and being in one never implies the other.
2. **Exclusion is destructive; evidence is not.** A wrong entry in `p3_exclusion_roots` makes real user files invisible to the entire product, with no observation, no fact and no review surface. A wrong entry in `p5_evidence_markers` produces one weak observation P6 can outweigh. The two arrays therefore have completely different bars for admission, and this file applies them differently on purpose.
3. **`p3_exclusion_roots` contains §1.1's four literal names and nothing else.** No addition was made. Every candidate considered is written up under *Uncertain* with the argument on both sides, so Joseph decides rather than discovers.
4. **Every `p5_evidence_markers` row carries one of exactly four `kind` values** — `repository marker`, `package manifest`, `notebook metadata`, `README file`. P5 raises `UnknownMarkerKind` on a fifth, so a row with any other kind is not merely wrong, it is unloadable.
5. **P5 emits markers; it does not read code.** §2.4: structural evidence "rather than forcing semantic analysis to infer a project from arbitrary code text". Nothing in this file inspects file contents beyond the notebook keys, and nothing concludes that a directory *is* a project — that is a fact, and facts are P6's.

## Injection

Two different injection points, matching the two owners.

**P3** receives `p3_exclusion_roots` as configuration on the scan, alongside §1.1's eleven literal directory names. It is data P3 reads, never a constant in P3's source.

**P5** never receives `p5_evidence_markers` at all. The markers reach P5 as *reader output*: a caller-supplied `read_text_document(path) -> TextDocument` returns `markers=(StructuralMarker(kind=…, value=…), …)`, and E3 places them at `zone = metadata` with the kind as the field label. For archives, E4 takes a caller-supplied `recognize_markers(member_paths)`. Either way the list lives in the caller, and P5 PLAN Task 20's runtime-introspection guard asserts that no marker file name appears in any module-level container inside `src/extractors/`.

## Coverage note

`p5_evidence_markers` covers roughly thirty language and build ecosystems plus the version-control, CI, container and lint conventions that mark a repository. It is broad on purpose: this side is cheap to be wrong on. `p3_exclusion_roots` is four rows and stays four rows — that side is expensive to be wrong on, and §1.1's four are the only ones the design actually settles.

Deliberately absent from both: `node_modules`, `.git`, `venv`, `build`, `dist`, `target`, `vendor`, `Pods`, `site-packages`, `Library`, `__pycache__`. Those are §1.1's **eleven literal directory names**, a third mechanism that P3 already implements and that neither array duplicates.

## `p3_exclusion_roots` — P3 skips descendants of a directory holding one of these

4 entries.

| id | match | match_kind | case sensitive | role | applies to | status | rationale | design cite | FP risk | example true | example false (must NOT match) |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `p3x-package-json` | `package.json` | exact | yes | project-root marker: P3 emits an R3 exclusion verdict for every **descendant** of the directory holding this file | filename | settled — named literally by §1.1 | One of §1.1's four literal markers. **This file also appears in `p5_evidence_markers` as `p5m-package-json`, and the two roles are different.** As a P3 exclusion root it means *do not scan below here* — the directory is a dependency tree, not a personal destination. As P5 evidence it means *this file looks like part of a project* — an observation about a file P3 did admit. One name, two jobs, two arrays; conflating them is what rule 5 of this task forbids. | §1.1 "It should also reject descendants of software project roots indicated by files such as `package.json`, `requirements.txt`, `Cargo.toml`, or `go.mod`. This prevents the proposal engine from mistaking a dependency subdirectory for a meaningful personal destination." | low | `package.json` | `package.json.bak` |
| `p3x-requirements-txt` | `requirements.txt` | exact | yes | project-root marker: P3 skips descendants | filename | settled — named literally by §1.1 | One of §1.1's four literal markers. Also present in `p5_evidence_markers` as `p5m-requirements-txt`, with the same two-role split. | §1.1 "It should also reject descendants of software project roots indicated by files such as `package.json`, `requirements.txt`, `Cargo.toml`, or `go.mod`. This prevents the proposal engine from mistaking a dependency subdirectory for a meaningful personal destination." | medium | `requirements.txt` | `requirements-for-the-grant.txt` |
| `p3x-cargo-toml` | `Cargo.toml` | exact | yes | project-root marker: P3 skips descendants | filename | settled — named literally by §1.1 | One of §1.1's four literal markers. Also present in `p5_evidence_markers` as `p5m-cargo-toml`. | §1.1 "It should also reject descendants of software project roots indicated by files such as `package.json`, `requirements.txt`, `Cargo.toml`, or `go.mod`. This prevents the proposal engine from mistaking a dependency subdirectory for a meaningful personal destination." | low | `Cargo.toml` | `cargo-notes.toml` |
| `p3x-go-mod` | `go.mod` | exact | yes | project-root marker: P3 skips descendants | filename | settled — named literally by §1.1 | One of §1.1's four literal markers. Also present in `p5_evidence_markers` as `p5m-go-mod`. | §1.1 "It should also reject descendants of software project roots indicated by files such as `package.json`, `requirements.txt`, `Cargo.toml`, or `go.mod`. This prevents the proposal engine from mistaking a dependency subdirectory for a meaningful personal destination." | low | `go.mod` | `go.modules.txt` |

## `p5_evidence_markers` — P5 evidence that a file looks like part of a project

118 entries.

| id | match | match_kind | case sensitive | kind | applies to | rationale | design cite | FP risk | example true | example false (must NOT match) |
|---|---|---|---|---|---|---|---|---|---|---|
| `p5m-package-json` | `package.json` | exact | no | package manifest | filename | npm / Node.js manifest. §2.4 names "package manifests" as a structural indicator class. Also §1.1's first exclusion-root marker — see the two-role note. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `package.json` | `my-package.json` |
| `p5m-package-lock-json` | `package-lock.json` | exact | no | package manifest | filename | npm manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `package-lock.json` | `my-package-lock.json` |
| `p5m-yarn-lock` | `yarn.lock` | exact | no | package manifest | filename | Yarn manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `yarn.lock` | `my-yarn.lock` |
| `p5m-pnpm-lock-yaml` | `pnpm-lock.yaml` | exact | no | package manifest | filename | pnpm manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `pnpm-lock.yaml` | `my-pnpm-lock.yaml` |
| `p5m-bun-lockb` | `bun.lockb` | exact | no | package manifest | filename | Bun manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `bun.lockb` | `my-bun.lockb` |
| `p5m-deno-json` | `deno.json` | exact | no | package manifest | filename | Deno manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `deno.json` | `my-deno.json` |
| `p5m-deno-lock` | `deno.lock` | exact | no | package manifest | filename | Deno manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `deno.lock` | `my-deno.lock` |
| `p5m-requirements-txt` | `requirements.txt` | exact | no | package manifest | filename | Python pip manifest. §2.4 names "package manifests" as a structural indicator class. Also §1.1's second exclusion-root marker. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `requirements.txt` | `my-requirements.txt` |
| `p5m-pyproject-toml` | `pyproject.toml` | exact | no | package manifest | filename | Python PEP 518 manifest. §2.4 names "package manifests" as a structural indicator class. The modern Python manifest, and a strong project-root signal §1.1 predates. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `pyproject.toml` | `my-pyproject.toml` |
| `p5m-setup-py` | `setup.py` | exact | no | package manifest | filename | Python setuptools manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `setup.py` | `my-setup.py` |
| `p5m-setup-cfg` | `setup.cfg` | exact | no | package manifest | filename | Python setuptools manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `setup.cfg` | `my-setup.cfg` |
| `p5m-pipfile` | `Pipfile` | exact | no | package manifest | filename | Python pipenv manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `Pipfile` | `my-Pipfile` |
| `p5m-pipfile-lock` | `Pipfile.lock` | exact | no | package manifest | filename | Python pipenv manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `Pipfile.lock` | `my-Pipfile.lock` |
| `p5m-poetry-lock` | `poetry.lock` | exact | no | package manifest | filename | Python Poetry manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `poetry.lock` | `my-poetry.lock` |
| `p5m-environment-yml` | `environment.yml` | exact | no | package manifest | filename | conda manifest. §2.4 names "package manifests" as a structural indicator class. Also used by non-Python conda environments. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `environment.yml` | `my-environment.yml` |
| `p5m-cargo-toml` | `Cargo.toml` | exact | no | package manifest | filename | Rust manifest. §2.4 names "package manifests" as a structural indicator class. Also §1.1's third exclusion-root marker. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `Cargo.toml` | `my-Cargo.toml` |
| `p5m-cargo-lock` | `Cargo.lock` | exact | no | package manifest | filename | Rust manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `Cargo.lock` | `my-Cargo.lock` |
| `p5m-go-mod` | `go.mod` | exact | no | package manifest | filename | Go manifest. §2.4 names "package manifests" as a structural indicator class. Also §1.1's fourth exclusion-root marker. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `go.mod` | `my-go.mod` |
| `p5m-go-sum` | `go.sum` | exact | no | package manifest | filename | Go manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `go.sum` | `my-go.sum` |
| `p5m-pom-xml` | `pom.xml` | exact | no | package manifest | filename | Maven / Java manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `pom.xml` | `my-pom.xml` |
| `p5m-build-gradle` | `build.gradle` | exact | no | package manifest | filename | Gradle manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `build.gradle` | `my-build.gradle` |
| `p5m-build-gradle-kts` | `build.gradle.kts` | exact | no | package manifest | filename | Gradle Kotlin DSL manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `build.gradle.kts` | `my-build.gradle.kts` |
| `p5m-settings-gradle` | `settings.gradle` | exact | no | package manifest | filename | Gradle manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `settings.gradle` | `my-settings.gradle` |
| `p5m-settings-gradle-kts` | `settings.gradle.kts` | exact | no | package manifest | filename | Gradle Kotlin DSL manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `settings.gradle.kts` | `my-settings.gradle.kts` |
| `p5m-gemfile` | `Gemfile` | exact | no | package manifest | filename | Ruby Bundler manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `Gemfile` | `my-Gemfile` |
| `p5m-gemfile-lock` | `Gemfile.lock` | exact | no | package manifest | filename | Ruby Bundler manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `Gemfile.lock` | `my-Gemfile.lock` |
| `p5m-composer-json` | `composer.json` | exact | no | package manifest | filename | PHP Composer manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `composer.json` | `my-composer.json` |
| `p5m-composer-lock` | `composer.lock` | exact | no | package manifest | filename | PHP Composer manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `composer.lock` | `my-composer.lock` |
| `p5m-cmakelists-txt` | `CMakeLists.txt` | exact | no | package manifest | filename | CMake / C++ manifest. §2.4 names "package manifests" as a structural indicator class. **Rule 5's worked example.** A C++ project marker, and deliberately *not* an exclusion root. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `CMakeLists.txt` | `my-CMakeLists.txt` |
| `p5m-makefile` | `Makefile` | exact | no | package manifest | filename | make manifest. §2.4 names "package manifests" as a structural indicator class. Common outside software too (LaTeX, docs, data pipelines), so it is evidence only. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | medium | `Makefile` | `my-Makefile` |
| `p5m-meson-build` | `meson.build` | exact | no | package manifest | filename | Meson manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `meson.build` | `my-meson.build` |
| `p5m-configure-ac` | `configure.ac` | exact | no | package manifest | filename | GNU Autotools manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `configure.ac` | `my-configure.ac` |
| `p5m-makefile-am` | `Makefile.am` | exact | no | package manifest | filename | GNU Automake manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `Makefile.am` | `my-Makefile.am` |
| `p5m-vcpkg-json` | `vcpkg.json` | exact | no | package manifest | filename | vcpkg / C++ manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `vcpkg.json` | `my-vcpkg.json` |
| `p5m-conanfile-txt` | `conanfile.txt` | exact | no | package manifest | filename | Conan / C++ manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `conanfile.txt` | `my-conanfile.txt` |
| `p5m-conanfile-py` | `conanfile.py` | exact | no | package manifest | filename | Conan / C++ manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `conanfile.py` | `my-conanfile.py` |
| `p5m-pubspec-yaml` | `pubspec.yaml` | exact | no | package manifest | filename | Dart / Flutter manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `pubspec.yaml` | `my-pubspec.yaml` |
| `p5m-mix-exs` | `mix.exs` | exact | no | package manifest | filename | Elixir manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `mix.exs` | `my-mix.exs` |
| `p5m-package-swift` | `Package.swift` | exact | no | package manifest | filename | Swift Package Manager manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `Package.swift` | `my-Package.swift` |
| `p5m-podfile` | `Podfile` | exact | no | package manifest | filename | CocoaPods manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `Podfile` | `my-Podfile` |
| `p5m-podfile-lock` | `Podfile.lock` | exact | no | package manifest | filename | CocoaPods manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `Podfile.lock` | `my-Podfile.lock` |
| `p5m-cartfile` | `Cartfile` | exact | no | package manifest | filename | Carthage manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `Cartfile` | `my-Cartfile` |
| `p5m-build-sbt` | `build.sbt` | exact | no | package manifest | filename | Scala sbt manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `build.sbt` | `my-build.sbt` |
| `p5m-project-clj` | `project.clj` | exact | no | package manifest | filename | Clojure Leiningen manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `project.clj` | `my-project.clj` |
| `p5m-deps-edn` | `deps.edn` | exact | no | package manifest | filename | Clojure tools.deps manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `deps.edn` | `my-deps.edn` |
| `p5m-rebar-config` | `rebar.config` | exact | no | package manifest | filename | Erlang manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `rebar.config` | `my-rebar.config` |
| `p5m-shard-yml` | `shard.yml` | exact | no | package manifest | filename | Crystal manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `shard.yml` | `my-shard.yml` |
| `p5m-stack-yaml` | `stack.yaml` | exact | no | package manifest | filename | Haskell Stack manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `stack.yaml` | `my-stack.yaml` |
| `p5m-cabal-project` | `cabal.project` | exact | no | package manifest | filename | Haskell Cabal manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `cabal.project` | `my-cabal.project` |
| `p5m-description` | `DESCRIPTION` | exact | yes | package manifest | filename | R package manifest. §2.4 names "package manifests" as a structural indicator class. R's manifest has no extension and the bare word is generic, so it is `exact` and case-sensitive. Case-sensitive: the lowercase word is ordinary English. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | medium | `DESCRIPTION` | `description` |
| `p5m-renv-lock` | `renv.lock` | exact | no | package manifest | filename | R renv manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `renv.lock` | `my-renv.lock` |
| `p5m-packages-config` | `packages.config` | exact | no | package manifest | filename | NuGet / .NET manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `packages.config` | `my-packages.config` |
| `p5m-directory-build-props` | `Directory.Build.props` | exact | no | package manifest | filename | MSBuild / .NET manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `Directory.Build.props` | `my-Directory.Build.props` |
| `p5m-global-json` | `global.json` | exact | no | package manifest | filename | .NET SDK manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `global.json` | `my-global.json` |
| `p5m-build-bazel` | `BUILD.bazel` | exact | no | package manifest | filename | Bazel manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `BUILD.bazel` | `my-BUILD.bazel` |
| `p5m-workspace` | `WORKSPACE` | exact | yes | package manifest | filename | Bazel manifest. §2.4 names "package manifests" as a structural indicator class. Bare uppercase word; case-sensitive exact only. Case-sensitive: the lowercase word is ordinary English. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | medium | `WORKSPACE` | `workspace` |
| `p5m-module-bazel` | `MODULE.bazel` | exact | no | package manifest | filename | Bazel manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `MODULE.bazel` | `my-MODULE.bazel` |
| `p5m-flake-nix` | `flake.nix` | exact | no | package manifest | filename | Nix flakes manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `flake.nix` | `my-flake.nix` |
| `p5m-default-nix` | `default.nix` | exact | no | package manifest | filename | Nix manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `default.nix` | `my-default.nix` |
| `p5m-shell-nix` | `shell.nix` | exact | no | package manifest | filename | Nix manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `shell.nix` | `my-shell.nix` |
| `p5m-chart-yaml` | `Chart.yaml` | exact | no | package manifest | filename | Helm manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `Chart.yaml` | `my-Chart.yaml` |
| `p5m-go-work` | `go.work` | exact | no | package manifest | filename | Go workspaces manifest. §2.4 names "package manifests" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `go.work` | `my-go.work` |
| `p5r-git` | `.git` | exact | no | repository marker | directory_name | Version control. Present in the *root* of a repository — but see the refusal: this must never become an exclusion root. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.git` | `notes-.git` |
| `p5r-gitignore` | `.gitignore` | exact | no | repository marker | filename | Version control config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.gitignore` | `notes-.gitignore` |
| `p5r-gitattributes` | `.gitattributes` | exact | no | repository marker | filename | Version control config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.gitattributes` | `notes-.gitattributes` |
| `p5r-gitmodules` | `.gitmodules` | exact | no | repository marker | filename | Submodule config; a strong multi-repo signal. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.gitmodules` | `notes-.gitmodules` |
| `p5r-hg` | `.hg` | exact | no | repository marker | directory_name | Mercurial. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.hg` | `notes-.hg` |
| `p5r-svn` | `.svn` | exact | no | repository marker | directory_name | Subversion. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.svn` | `notes-.svn` |
| `p5r-github` | `.github` | exact | no | repository marker | directory_name | GitHub workflows and templates. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.github` | `notes-.github` |
| `p5r-gitlab-ci-yml` | `.gitlab-ci.yml` | exact | no | repository marker | filename | GitLab CI. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.gitlab-ci.yml` | `notes-.gitlab-ci.yml` |
| `p5r-travis-yml` | `.travis.yml` | exact | no | repository marker | filename | Travis CI. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.travis.yml` | `notes-.travis.yml` |
| `p5r-azure-pipelines-yml` | `azure-pipelines.yml` | exact | no | repository marker | filename | Azure Pipelines. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `azure-pipelines.yml` | `notes-azure-pipelines.yml` |
| `p5r-jenkinsfile` | `Jenkinsfile` | exact | no | repository marker | filename | Jenkins. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `Jenkinsfile` | `notes-Jenkinsfile` |
| `p5r-circleci` | `.circleci` | exact | no | repository marker | directory_name | CircleCI config directory. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.circleci` | `notes-.circleci` |
| `p5r-dockerfile` | `Dockerfile` | exact | no | repository marker | filename | Container build definition. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `Dockerfile` | `notes-Dockerfile` |
| `p5r-docker-compose-yml` | `docker-compose.yml` | exact | no | repository marker | filename | Container orchestration. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `docker-compose.yml` | `notes-docker-compose.yml` |
| `p5r-docker-compose-yaml` | `docker-compose.yaml` | exact | no | repository marker | filename | Container orchestration. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `docker-compose.yaml` | `notes-docker-compose.yaml` |
| `p5r-dockerignore` | `.dockerignore` | exact | no | repository marker | filename | Container build config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.dockerignore` | `notes-.dockerignore` |
| `p5r-editorconfig` | `.editorconfig` | exact | no | repository marker | filename | Editor config, near-universal in repositories. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.editorconfig` | `notes-.editorconfig` |
| `p5r-pre-commit-config-yaml` | `.pre-commit-config.yaml` | exact | no | repository marker | filename | Pre-commit hooks. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.pre-commit-config.yaml` | `notes-.pre-commit-config.yaml` |
| `p5r-tsconfig-json` | `tsconfig.json` | exact | no | repository marker | filename | TypeScript project config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `tsconfig.json` | `notes-tsconfig.json` |
| `p5r-jsconfig-json` | `jsconfig.json` | exact | no | repository marker | filename | JavaScript project config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `jsconfig.json` | `notes-jsconfig.json` |
| `p5r-eslintrc-json` | `.eslintrc.json` | exact | no | repository marker | filename | Lint config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.eslintrc.json` | `notes-.eslintrc.json` |
| `p5r-eslintrc-js` | `.eslintrc.js` | exact | no | repository marker | filename | Lint config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.eslintrc.js` | `notes-.eslintrc.js` |
| `p5r-eslintrc-cjs` | `.eslintrc.cjs` | exact | no | repository marker | filename | Lint config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.eslintrc.cjs` | `notes-.eslintrc.cjs` |
| `p5r-eslint-config-js` | `eslint.config.js` | exact | no | repository marker | filename | Flat lint config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `eslint.config.js` | `notes-eslint.config.js` |
| `p5r-prettierrc` | `.prettierrc` | exact | no | repository marker | filename | Format config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `.prettierrc` | `notes-.prettierrc` |
| `p5r-vite-config-ts` | `vite.config.ts` | exact | no | repository marker | filename | Build config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `vite.config.ts` | `notes-vite.config.ts` |
| `p5r-vite-config-js` | `vite.config.js` | exact | no | repository marker | filename | Build config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `vite.config.js` | `notes-vite.config.js` |
| `p5r-webpack-config-js` | `webpack.config.js` | exact | no | repository marker | filename | Build config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `webpack.config.js` | `notes-webpack.config.js` |
| `p5r-rollup-config-js` | `rollup.config.js` | exact | no | repository marker | filename | Build config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `rollup.config.js` | `notes-rollup.config.js` |
| `p5r-babel-config-js` | `babel.config.js` | exact | no | repository marker | filename | Transpiler config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `babel.config.js` | `notes-babel.config.js` |
| `p5r-tox-ini` | `tox.ini` | exact | no | repository marker | filename | Python test matrix config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `tox.ini` | `notes-tox.ini` |
| `p5r-pytest-ini` | `pytest.ini` | exact | no | repository marker | filename | Python test config. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `pytest.ini` | `notes-pytest.ini` |
| `p5r-noxfile-py` | `noxfile.py` | exact | no | repository marker | filename | Python task runner. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `noxfile.py` | `notes-noxfile.py` |
| `p5r-license` | `LICENSE` | exact | yes | repository marker | filename | Bare uppercase word; exact and case-sensitive. §2.4 names "repository markers" as a structural indicator class. Case-sensitive: the lowercase word is ordinary English. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | medium | `LICENSE` | `license` |
| `p5r-license-md` | `LICENSE.md` | exact | no | repository marker | filename | Licence file. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `LICENSE.md` | `notes-LICENSE.md` |
| `p5r-license-txt` | `LICENSE.txt` | exact | no | repository marker | filename | Licence file. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `LICENSE.txt` | `notes-LICENSE.txt` |
| `p5r-copying` | `COPYING` | exact | yes | repository marker | filename | GNU-convention licence file. §2.4 names "repository markers" as a structural indicator class. Case-sensitive: the lowercase word is ordinary English. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | medium | `COPYING` | `copying` |
| `p5r-contributing-md` | `CONTRIBUTING.md` | exact | no | repository marker | filename | Repository convention file. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `CONTRIBUTING.md` | `notes-CONTRIBUTING.md` |
| `p5r-code_of_conduct-md` | `CODE_OF_CONDUCT.md` | exact | no | repository marker | filename | Repository convention file. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `CODE_OF_CONDUCT.md` | `notes-CODE_OF_CONDUCT.md` |
| `p5r-changelog-md` | `CHANGELOG.md` | exact | no | repository marker | filename | Repository convention file. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `CHANGELOG.md` | `notes-CHANGELOG.md` |
| `p5r-security-md` | `SECURITY.md` | exact | no | repository marker | filename | Repository convention file. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `SECURITY.md` | `notes-SECURITY.md` |
| `p5r-codeowners` | `CODEOWNERS` | exact | yes | repository marker | filename | Repository ownership file. §2.4 names "repository markers" as a structural indicator class. Case-sensitive: the lowercase word is ordinary English. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `CODEOWNERS` | `codeowners` |
| `p5r-src` | `src` | exact | no | repository marker | directory_name | **§2.5 names this literally** as one of the four things a source-code archive may reveal. Generic on its own; it is one marker among several, never a verdict. §2.4 names "repository markers" as a structural indicator class. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | high | `src` | `source` |
| `p5r-__init__-py` | `__init__.py` | exact | no | repository marker | filename | **§2.5's "Python package layout"**, made concrete: the file that makes a directory a Python package. §2.4 names "repository markers" as a structural indicator class. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `__init__.py` | `notes-__init__.py` |
| `p5r-py-typed` | `py.typed` | exact | no | repository marker | filename | PEP 561 typing marker inside a Python package. §2.4 names "repository markers" as a structural indicator class. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `py.typed` | `notes-py.typed` |
| `p5d-readme-md` | `README.md` | exact | no | README file | filename | The §2.5 literal. Markdown is the dominant form. §2.4 names "README files" as a structural indicator class; §2.5 names `README.md` literally. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `README.md` | `READMEs for the team` |
| `p5d-readme` | `README` | exact | yes | README file | filename | Extensionless form; case-sensitive exact. §2.4 names "README files" as a structural indicator class; §2.5 names `README.md` literally. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | medium | `README` | `READMEs for the team` |
| `p5d-readme-txt` | `README.txt` | exact | no | README file | filename | Plain-text form. §2.4 names "README files" as a structural indicator class; §2.5 names `README.md` literally. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `README.txt` | `READMEs for the team` |
| `p5d-readme-rst` | `README.rst` | exact | no | README file | filename | reStructuredText, common in Python projects. §2.4 names "README files" as a structural indicator class; §2.5 names `README.md` literally. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `README.rst` | `READMEs for the team` |
| `p5d-readme-adoc` | `README.adoc` | exact | no | README file | filename | AsciiDoc form. §2.4 names "README files" as a structural indicator class; §2.5 names `README.md` literally. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `README.adoc` | `READMEs for the team` |
| `p5d-readme-md-2` | `readme.md` | exact | no | README file | filename | Lowercase form — listed separately because the marker `value` is stored verbatim (P5 `StructuralMarker.value`), so the two spellings are two different recorded values even though matching is case-insensitive. §2.4 names "README files" as a structural indicator class; §2.5 names `README.md` literally. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `readme.md` | `READMEs for the team` |
| `p5n-nbformat` | `nbformat` | exact | yes | notebook metadata | notebook_json_key | The notebook format version. Its presence at the top level of a JSON document is what makes that document a notebook rather than arbitrary JSON. §2.4 names "notebook metadata" as a structural indicator class. The `value` P5 stores is the metadata key verbatim, not a filename — `StructuralMarker.value` is documented as "a file name, a manifest name, a notebook metadata key". | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `nbformat` | `notebook` |
| `p5n-nbformat-minor` | `nbformat_minor` | exact | yes | notebook metadata | notebook_json_key | Notebook format minor version. §2.4 names "notebook metadata" as a structural indicator class. The `value` P5 stores is the metadata key verbatim, not a filename — `StructuralMarker.value` is documented as "a file name, a manifest name, a notebook metadata key". | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `nbformat_minor` | `notebook` |
| `p5n-kernelspec` | `kernelspec` | exact | yes | notebook metadata | notebook_json_key | The kernel the notebook runs on; carries `name`, `display_name`, `language`. §2.4 names "notebook metadata" as a structural indicator class. The `value` P5 stores is the metadata key verbatim, not a filename — `StructuralMarker.value` is documented as "a file name, a manifest name, a notebook metadata key". | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `kernelspec` | `notebook` |
| `p5n-language-info` | `language_info` | exact | yes | notebook metadata | notebook_json_key | The notebook's language and version metadata — §2.4's "language where relevant" for notebooks. §2.4 names "notebook metadata" as a structural indicator class. The `value` P5 stores is the metadata key verbatim, not a filename — `StructuralMarker.value` is documented as "a file name, a manifest name, a notebook metadata key". | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `language_info` | `notebook` |
| `p5n-cells` | `cells` | exact | yes | notebook metadata | notebook_json_key | The cell array. §2.9 names "notebook cell types" as notebook structure; P4's `cell` segment kind addresses them. §2.4 names "notebook metadata" as a structural indicator class. The `value` P5 stores is the metadata key verbatim, not a filename — `StructuralMarker.value` is documented as "a file name, a manifest name, a notebook metadata key". | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files" | low | `cells` | `notebook` |

## Refused — deliberately NOT matched

4 entries.

| id | match | match_kind | case sensitive | role | applies to | status | rationale | design cite | FP risk | example true | example false (must NOT match) |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `ref-cmakelists-as-exclusion` | `CMakeLists.txt` | exact | yes | REFUSED as a `p3_exclusion_root`; present instead as `p5m-cmakelists-txt` | filename | refused | The task's named worked example, and the clearest case for the split. `CMakeLists.txt` sits at the top of hand-written C++ source trees that a person authored and would want organized. Making it an exclusion root would make P3 emit an R3 verdict for every descendant — the user's own code would silently vanish from the corpus, and §1.1's stated purpose is the opposite: stopping the engine "mistaking a dependency subdirectory for a meaningful personal destination". A dependency tree is what §1.1 excludes; a source tree is not one. As P5 evidence the same file is useful and harmless. | §1.1 "It should also reject descendants of software project roots indicated by files such as `package.json`, `requirements.txt`, `Cargo.toml`, or `go.mod`. This prevents the proposal engine from mistaking a dependency subdirectory for a meaningful personal destination." | would be severe | `—` | `CMakeLists.txt` |
| `ref-git-as-exclusion-root` | `.git` | exact | yes | REFUSED as a `p3_exclusion_root`; present as `p5r-git` | directory_name | refused | Tempting and wrong. A directory containing `.git` is a repository root — but `.git` is already one of §1.1's **eleven literal directory names**, which means P3 skips the `.git` directory itself, not its parent's descendants. Promoting it to an exclusion root would exclude every git repository, and people keep notes, papers, theses and photo archives in git. This would delete more of a real corpus than any other candidate on this page. | §1.1's eleven literal names include `.git`; the project-root rule is a separate mechanism | would be catastrophic | `—` | `.git` |
| `ref-makefile-as-exclusion` | `Makefile` | exact | yes | REFUSED as a `p3_exclusion_root`; present as `p5m-makefile` | filename | refused | `Makefile` appears at the top of LaTeX theses, documentation sites, data pipelines and personal scripts as often as it does in software. Excluding its descendants would remove exactly the academic material this product exists to organize. | §1.1 "It should also reject descendants of software project roots indicated by files such as `package.json`, `requirements.txt`, `Cargo.toml`, or `go.mod`. This prevents the proposal engine from mistaking a dependency subdirectory for a meaningful personal destination." | would be severe | `—` | `Makefile` |
| `ref-readme-as-exclusion` | `README.md` | exact | yes | REFUSED as a `p3_exclusion_root`; present as `p5d-readme-md` | filename | refused | A README marks a directory a person documented — which is a reason to look *more* carefully, not to stop scanning. §2.5 names it as project **evidence**, never as an exclusion. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | would be catastrophic | `—` | `README.md` |

## Uncertain — needs Joseph

6 entries.

| id | match | match_kind | case sensitive | role | applies to | status | rationale | design cite | FP risk | example true | example false (must NOT match) |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `unc-pyproject-as-exclusion` | `pyproject.toml` | exact | yes | candidate `p3_exclusion_root` — NOT added | filename | proposed — needs Joseph | The strongest candidate for extending §1.1's four, and still not added. §1.1's list is explicitly open ("files such as"), it names `requirements.txt`, and `pyproject.toml` has since replaced it as the Python project root marker — so the *intent* of §1.1 arguably covers it. Against: a `pyproject.toml` sits at the top of hand-written analysis code and thesis tooling as well as dependency trees, and excluding descendants would drop them. **Decision needed from Joseph, framed narrowly:** should a directory holding `pyproject.toml` be skipped the way one holding `requirements.txt` is? If yes it is a one-line addition here with `status: proposed` removed; the default answer this file ships with is no. | §1.1 "It should also reject descendants of software project roots indicated by files such as `package.json`, `requirements.txt`, `Cargo.toml`, or `go.mod`. This prevents the proposal engine from mistaking a dependency subdirectory for a meaningful personal destination." — "files such as" signals an extensible set | medium | `pyproject.toml` | `pyproject-notes.toml` |
| `unc-lockfiles-as-exclusion` | `package-lock.json / yarn.lock / poetry.lock / Gemfile.lock / composer.lock` | exact | yes | candidate `p3_exclusion_roots` — NOT added | filename | proposed — needs Joseph | A lock file is a *stronger* dependency-tree signal than a manifest: it is machine-generated and never hand-authored, so a directory holding one is far more likely to be a real dependency tree than a personal folder. That makes lock files the safest possible extension of §1.1's four. Not added anyway, because a lock file always sits beside its manifest, so adding them changes almost no verdict while widening the rule that hides user trees. Recorded so the reasoning is on the record rather than re-derived later. | §1.1 "It should also reject descendants of software project roots indicated by files such as `package.json`, `requirements.txt`, `Cargo.toml`, or `go.mod`. This prevents the proposal engine from mistaking a dependency subdirectory for a meaningful personal destination." | low | `package-lock.json` | `my-lock.json` |
| `unc-other-ecosystems-as-exclusion` | `pom.xml / build.gradle / Gemfile / composer.json / mix.exs / pubspec.yaml` | exact | yes | candidate `p3_exclusion_roots` — NOT added | filename | proposed — needs Joseph | The obvious per-ecosystem extension of §1.1's four. All are in `p5_evidence_markers` already, which is where the task says extra language ecosystems go first. Adding any of them to the exclusion side hides whole trees, and none of them is a case where Joseph would *clearly* want the folder never scanned — which is the bar the task sets. The honest position: revisit after a real scan shows how many files each would remove. | §1.1 "It should also reject descendants of software project roots indicated by files such as `package.json`, `requirements.txt`, `Cargo.toml`, or `go.mod`. This prevents the proposal engine from mistaking a dependency subdirectory for a meaningful personal destination." | medium | `pom.xml` | `pom-notes.xml` |
| `unc-p3-oq9` | `does the marker-bearing directory itself get excluded?` | exact | no | open question inherited from P3 | n/a | open — P3 SPEC Open Question 9 | P3 SPEC Open Question 9: "Does the project-root rule exclude the root directory itself, or only its descendants? §1.1 says 'descendants of software project roots.' Whether the marker-bearing directory can still be a candidate root is unsettled." This catalogue supplies the marker names and takes no position on the question — it is P3's, and answering it here would be inventing. | P3 SPEC Open Question 9 | n/a | `—` | `—` |
| `unc-xcode-bundles` | `.xcodeproj / .xcworkspace` | exact | yes | neither array — already covered elsewhere | directory_name | open | These are directory bundles, and P3's ratified protected-container rule already refuses to descend into macOS packages — "P3 does not descend into one, does not stat its contents, does not hash a byte of it". Adding them here would duplicate a stronger rule in a weaker place. Recorded so nobody adds them later thinking they were missed. | P3 SPEC, protected containers (ratified 2026-08-20, closing Q7) | n/a | `—` | `MyApp.xcodeproj` |
| `unc-notebook-key-collision` | `cells / kernelspec as notebook metadata keys` | exact | yes | `p5_evidence_markers`, kind `notebook metadata` | notebook_json_key | open | These five keys are matched against **top-level JSON object keys**, not filenames — a different match target from every other row in this file, which is why each carries `applies_to: notebook_json_key`. An arbitrary JSON document could contain a top-level `cells` key and be mistaken for a notebook. `nbformat` is the discriminating one and should probably be required before the others are reported; that is the reader's logic, not this catalogue's, so it is flagged rather than decided. | §2.4 "structural indicators such as repository markers, package manifests, notebook metadata, and README files"; P5 `StructuralMarker.value` — "a file name, a manifest name, a notebook metadata key" | medium | `nbformat` | `cells` |

## Sources

- [npm — package.json documentation](https://docs.npmjs.com/cli/v10/configuring-npm/package-json) — **[SEARCH SUMMARY — not opened]** consulted 2026-08-20 — Vendor definition of the Node manifest.
- [Python Packaging — pyproject.toml specification](https://packaging.python.org/en/latest/specifications/pyproject-toml/) — **[SEARCH SUMMARY — not opened]** consulted 2026-08-20 — PEP 518/621 project manifest — the basis for `unc-pyproject-as-exclusion`.
- [Rust — The Cargo Book, the manifest format](https://doc.rust-lang.org/cargo/reference/manifest.html) — **[SEARCH SUMMARY — not opened]** consulted 2026-08-20 — Vendor definition of `Cargo.toml`.
- [Go — Go Modules Reference, go.mod files](https://go.dev/ref/mod#go-mod-file) — **[SEARCH SUMMARY — not opened]** consulted 2026-08-20 — Vendor definition of `go.mod`.
- [Jupyter — Notebook format (nbformat) specification](https://nbformat.readthedocs.io/en/latest/format_description.html) — **[SEARCH SUMMARY — not opened]** consulted 2026-08-20 — Defines the top-level keys `nbformat`, `nbformat_minor`, `metadata` (`kernelspec`, `language_info`) and `cells` — the five notebook-metadata rows.
- [Git — gitrepository-layout](https://git-scm.com/docs/gitrepository-layout) — **[SEARCH SUMMARY — not opened]** consulted 2026-08-20 — Defines the `.git` directory whose promotion to an exclusion root is refused.

---

_Generated from the JSON beside this file by `render.py`. Do not hand-edit: edit the JSON and re-run._
