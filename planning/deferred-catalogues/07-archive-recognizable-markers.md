# 07 — Archive recognizable markers (P5 E4's `recognize_markers`)

- **list_id**: archive_recognizable_markers
- **version**: 1.0
- **authored**: 2026-08-20
- **owner**: P5 (injected) — the observations it produces are consumed by P6
- **consumer**: the caller-supplied `recognize_markers(member_paths) -> tuple[ArchiveMarker, ...]` that P5's `extract_archive` requires. Each marker becomes one observation at `zone = metadata`, `field = <the marker's kind>`, `reliability: direct`, whose `raw_value` is **the member path that carries the marker** — not the marker word.
- **match_field**: each entry in the archive manifest's member-path list, read from the manifest. `applies_to` says whether a row matches the member's basename or a path segment.
- **normalization for matching**: Basename comparison after Unicode NFC; case-insensitive except where a row says otherwise. Document-name rows additionally strip the extension and treat `_`, `-` and space as equivalent separators. **Nothing is decompressed to perform any of this** — the member paths come from the manifest, and §2.5's absolute prohibition on extraction is not weakened by a single row here.
- **kind vocabulary**: P5 defines `MARKER_KINDS = ("source-code manifest", "document name")` — §2.5's own two classes — and raises `UnknownMarkerKind` on anything else. Every row therefore carries one of exactly those two strings. **A naming stretch worth flagging:** §2.5 names `README.md`, a `src` directory, and "Python package layout" as things a source-code archive reveals, and none of them is literally a *manifest*. They are kinded `source-code manifest` anyway, because that is the only code-side class P5 accepts and inventing a third is a runtime error, not a style choice.
- **sourcing**: **How to read the `verification` column, because it is not decoration.** Only five pages were opened and read directly while authoring these catalogues; three more were opened by the catalogues-finish agent, who supplied verbatim quotes. Everything else is marked `search_summary`: the fact came from a web-search result summary and the page itself was not retrieved. Those rows say `consulted` rather than `retrieved`, because claiming a retrieval date for a page nobody opened is exactly the kind of unearned confidence these catalogues exist to keep out of the product. A `search_summary` source is not worthless — it is how most of the vendor-prefix and manifest-name knowledge here was gathered — but it must not be quoted as if read, and a row that would be harmful if wrong should be upgraded before launch.

## Design basis

- §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project."
- §2.5 "A ZIP file named `submission.zip` may contain a transcript, personal statement, resume, certificate, and form, which is meaningful evidence of a purpose-defined application packet even when the outer archive name is vague."
- §2.5: "The engine should read and store the archive type, contained paths, filenames, folder names, extensions, file count, uncompressed size where available, and recognizable markers such as source-code manifests or document names."
- §2.5, the prohibition this file operates under: "the normal scan should never extract archive contents to the filesystem, because doing so creates security, storage, and side-effect risks."
- P5 SPEC Deferred: "Archive 'recognizable markers' beyond the above | §2.5 | That manifests and document names are markers | The marker set" — this file is the missing column.
- P5 PLAN Task 13: `MARKER_KINDS: tuple[str, ...] = ("source-code manifest", "document name")`; `UnknownMarkerKind` is raised for any other class; "WHICH files are markers is Deferred … so no member name appears here and the recognizer is caller-supplied."

## Rules this list obeys

1. **Manifest only, never extraction.** Every row matches a *path string* from the archive manifest. Nothing in this file requires reading a member's bytes, and §2.5's prohibition — never extract to the filesystem, never let a nested or oversized archive become a decompression bomb — is absolute and untouched.
2. **Exactly two kinds.** `source-code manifest` and `document name`. A third value is not a modelling choice, it is `UnknownMarkerKind` at run time.
3. **The `raw_value` is the member path, not the marker.** P5 PLAN Task 13 emits `raw_value = marker.member_path`, so an archive containing `project/package.json` stores that path verbatim. The kind goes in the field label. This is what keeps the observation a *reading* rather than a conclusion.
4. **The document-name vocabulary stops at §2.5's five.** transcript, personal statement, resume, certificate, form. Growing it into a general document-type vocabulary is the 200-300 template library, which is §5.7's, deferred, and **P10's** — not this file's, and not something to seed by accident from the archive side.
5. **Co-occurrence is the evidence, not any single marker.** §2.5's worked case is `submission.zip` containing *five* documents together. One `form` in an archive means very little. E4 emits each marker as its own observation and never counts them; whether five markers together mean an application packet is a purpose fact (§3.9) and P6's.
6. **No archive-name inference.** §2.5's point is that the outer name `submission.zip` is *vague* and the members are what carry the evidence. Nothing here matches the archive's own filename.

## Injection

`extract_archive(..., read_manifest=…, recognize_markers=make_archive_recognizer(load('07-archive-recognizable-markers.json')))`. P5 PLAN Task 13 is explicit that "no member name appears here and the recognizer is caller-supplied", and Task 20's runtime-introspection guard asserts that no marker file name exists in any module-level container inside `src/extractors/`. E4 additionally validates every returned `kind` against `MARKER_KINDS` and raises `UnknownMarkerKind` otherwise, so a malformed catalogue fails loudly at the boundary rather than silently producing a bad observation.

## Coverage note

Two arrays in one list, matching §2.5's two classes. The **source-code manifest** side is derived mechanically from catalogue 05 — the `package manifest` and `README file` rows plus `src`, `__init__.py`, `py.typed`, `.git`, `.gitignore`, `.gitmodules` and `Dockerfile` — so the two catalogues cannot disagree about what a project marker is. The **document name** side is exactly §2.5's five names plus same-document synonyms, and deliberately goes no further.

**What is not here, and why.** Office-format internals (`word/document.xml`, `xl/workbook.xml`, `[Content_Types].xml`) would identify a `.docx` opened as a ZIP — but that is *routing*, and §2.9 gives routing to R "signature over extension", not to E4's marker recogniser. Bomb signals are not markers either: §2.5 makes uncompressed size a safety input that E4 already reads from the manifest, and P5 PLAN Task 13 already specifies the `unreadable`/`partial` outcomes.

## Entries

80 entries.

| id | match | pattern | match_kind | case sensitive | kind | applies to | rationale | design cite | FP risk | example true | example false (must NOT match) |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `arc-package-json` | `package.json` | — | exact | no | source-code manifest | member_basename | §2.5 names this literally as one of the four things a source-code archive may reveal. Derived from catalogue 05 row `p5m-package-json` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/package.json` | `notes/package.json.bak` |
| `arc-package-lock-json` | `package-lock.json` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-package-lock-json` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/package-lock.json` | `notes/package-lock.json.bak` |
| `arc-yarn-lock` | `yarn.lock` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-yarn-lock` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/yarn.lock` | `notes/yarn.lock.bak` |
| `arc-pnpm-lock-yaml` | `pnpm-lock.yaml` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-pnpm-lock-yaml` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/pnpm-lock.yaml` | `notes/pnpm-lock.yaml.bak` |
| `arc-bun-lockb` | `bun.lockb` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-bun-lockb` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/bun.lockb` | `notes/bun.lockb.bak` |
| `arc-deno-json` | `deno.json` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-deno-json` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/deno.json` | `notes/deno.json.bak` |
| `arc-deno-lock` | `deno.lock` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-deno-lock` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/deno.lock` | `notes/deno.lock.bak` |
| `arc-requirements-txt` | `requirements.txt` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-requirements-txt` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/requirements.txt` | `notes/requirements.txt.bak` |
| `arc-pyproject-toml` | `pyproject.toml` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-pyproject-toml` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/pyproject.toml` | `notes/pyproject.toml.bak` |
| `arc-setup-py` | `setup.py` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-setup-py` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/setup.py` | `notes/setup.py.bak` |
| `arc-setup-cfg` | `setup.cfg` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-setup-cfg` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/setup.cfg` | `notes/setup.cfg.bak` |
| `arc-pipfile` | `Pipfile` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-pipfile` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/Pipfile` | `notes/Pipfile.bak` |
| `arc-pipfile-lock` | `Pipfile.lock` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-pipfile-lock` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/Pipfile.lock` | `notes/Pipfile.lock.bak` |
| `arc-poetry-lock` | `poetry.lock` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-poetry-lock` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/poetry.lock` | `notes/poetry.lock.bak` |
| `arc-environment-yml` | `environment.yml` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-environment-yml` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/environment.yml` | `notes/environment.yml.bak` |
| `arc-cargo-toml` | `Cargo.toml` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-cargo-toml` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/Cargo.toml` | `notes/Cargo.toml.bak` |
| `arc-cargo-lock` | `Cargo.lock` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-cargo-lock` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/Cargo.lock` | `notes/Cargo.lock.bak` |
| `arc-go-mod` | `go.mod` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-go-mod` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/go.mod` | `notes/go.mod.bak` |
| `arc-go-sum` | `go.sum` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-go-sum` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/go.sum` | `notes/go.sum.bak` |
| `arc-pom-xml` | `pom.xml` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-pom-xml` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/pom.xml` | `notes/pom.xml.bak` |
| `arc-build-gradle` | `build.gradle` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-build-gradle` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/build.gradle` | `notes/build.gradle.bak` |
| `arc-build-gradle-kts` | `build.gradle.kts` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-build-gradle-kts` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/build.gradle.kts` | `notes/build.gradle.kts.bak` |
| `arc-settings-gradle` | `settings.gradle` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-settings-gradle` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/settings.gradle` | `notes/settings.gradle.bak` |
| `arc-settings-gradle-kts` | `settings.gradle.kts` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-settings-gradle-kts` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/settings.gradle.kts` | `notes/settings.gradle.kts.bak` |
| `arc-gemfile` | `Gemfile` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-gemfile` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/Gemfile` | `notes/Gemfile.bak` |
| `arc-gemfile-lock` | `Gemfile.lock` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-gemfile-lock` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/Gemfile.lock` | `notes/Gemfile.lock.bak` |
| `arc-composer-json` | `composer.json` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-composer-json` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/composer.json` | `notes/composer.json.bak` |
| `arc-composer-lock` | `composer.lock` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-composer-lock` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/composer.lock` | `notes/composer.lock.bak` |
| `arc-cmakelists-txt` | `CMakeLists.txt` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-cmakelists-txt` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/CMakeLists.txt` | `notes/CMakeLists.txt.bak` |
| `arc-makefile` | `Makefile` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-makefile` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | medium | `project/Makefile` | `notes/Makefile.bak` |
| `arc-meson-build` | `meson.build` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-meson-build` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/meson.build` | `notes/meson.build.bak` |
| `arc-configure-ac` | `configure.ac` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-configure-ac` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/configure.ac` | `notes/configure.ac.bak` |
| `arc-makefile-am` | `Makefile.am` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-makefile-am` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/Makefile.am` | `notes/Makefile.am.bak` |
| `arc-vcpkg-json` | `vcpkg.json` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-vcpkg-json` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/vcpkg.json` | `notes/vcpkg.json.bak` |
| `arc-conanfile-txt` | `conanfile.txt` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-conanfile-txt` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/conanfile.txt` | `notes/conanfile.txt.bak` |
| `arc-conanfile-py` | `conanfile.py` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-conanfile-py` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/conanfile.py` | `notes/conanfile.py.bak` |
| `arc-pubspec-yaml` | `pubspec.yaml` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-pubspec-yaml` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/pubspec.yaml` | `notes/pubspec.yaml.bak` |
| `arc-mix-exs` | `mix.exs` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-mix-exs` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/mix.exs` | `notes/mix.exs.bak` |
| `arc-package-swift` | `Package.swift` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-package-swift` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/Package.swift` | `notes/Package.swift.bak` |
| `arc-podfile` | `Podfile` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-podfile` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/Podfile` | `notes/Podfile.bak` |
| `arc-podfile-lock` | `Podfile.lock` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-podfile-lock` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/Podfile.lock` | `notes/Podfile.lock.bak` |
| `arc-cartfile` | `Cartfile` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-cartfile` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/Cartfile` | `notes/Cartfile.bak` |
| `arc-build-sbt` | `build.sbt` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-build-sbt` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/build.sbt` | `notes/build.sbt.bak` |
| `arc-project-clj` | `project.clj` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-project-clj` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/project.clj` | `notes/project.clj.bak` |
| `arc-deps-edn` | `deps.edn` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-deps-edn` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/deps.edn` | `notes/deps.edn.bak` |
| `arc-rebar-config` | `rebar.config` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-rebar-config` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/rebar.config` | `notes/rebar.config.bak` |
| `arc-shard-yml` | `shard.yml` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-shard-yml` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/shard.yml` | `notes/shard.yml.bak` |
| `arc-stack-yaml` | `stack.yaml` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-stack-yaml` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/stack.yaml` | `notes/stack.yaml.bak` |
| `arc-cabal-project` | `cabal.project` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-cabal-project` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/cabal.project` | `notes/cabal.project.bak` |
| `arc-description` | `DESCRIPTION` | — | exact | yes | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-description` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | medium | `project/DESCRIPTION` | `notes/DESCRIPTION.bak` |
| `arc-renv-lock` | `renv.lock` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-renv-lock` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/renv.lock` | `notes/renv.lock.bak` |
| `arc-packages-config` | `packages.config` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-packages-config` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/packages.config` | `notes/packages.config.bak` |
| `arc-directory-build-props` | `Directory.Build.props` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-directory-build-props` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/Directory.Build.props` | `notes/Directory.Build.props.bak` |
| `arc-global-json` | `global.json` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-global-json` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/global.json` | `notes/global.json.bak` |
| `arc-build-bazel` | `BUILD.bazel` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-build-bazel` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/BUILD.bazel` | `notes/BUILD.bazel.bak` |
| `arc-workspace` | `WORKSPACE` | — | exact | yes | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-workspace` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | medium | `project/WORKSPACE` | `notes/WORKSPACE.bak` |
| `arc-module-bazel` | `MODULE.bazel` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-module-bazel` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/MODULE.bazel` | `notes/MODULE.bazel.bak` |
| `arc-flake-nix` | `flake.nix` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-flake-nix` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/flake.nix` | `notes/flake.nix.bak` |
| `arc-default-nix` | `default.nix` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-default-nix` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/default.nix` | `notes/default.nix.bak` |
| `arc-shell-nix` | `shell.nix` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-shell-nix` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/shell.nix` | `notes/shell.nix.bak` |
| `arc-chart-yaml` | `Chart.yaml` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-chart-yaml` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/Chart.yaml` | `notes/Chart.yaml.bak` |
| `arc-go-work` | `go.work` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5m-go-work` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/go.work` | `notes/go.work.bak` |
| `arc-git` | `.git` | — | exact | no | source-code manifest | member_path_segment | Derived from catalogue 05 row `p5r-git` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/.git/main.py` | `notes/.git.bak` |
| `arc-gitignore` | `.gitignore` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5r-gitignore` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/.gitignore` | `notes/.gitignore.bak` |
| `arc-gitmodules` | `.gitmodules` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5r-gitmodules` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/.gitmodules` | `notes/.gitmodules.bak` |
| `arc-dockerfile` | `Dockerfile` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5r-dockerfile` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/Dockerfile` | `notes/Dockerfile.bak` |
| `arc-src` | `src` | — | exact | no | source-code manifest | member_path_segment | §2.5 names this literally as one of the four things a source-code archive may reveal. Derived from catalogue 05 row `p5r-src` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | high | `project/src/main.py` | `notes/src.bak` |
| `arc-__init__-py` | `__init__.py` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5r-__init__-py` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/__init__.py` | `notes/__init__.py.bak` |
| `arc-py-typed` | `py.typed` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5r-py-typed` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/py.typed` | `notes/py.typed.bak` |
| `arc-readme-md` | `README.md` | — | exact | no | source-code manifest | member_basename | §2.5 names this literally as one of the four things a source-code archive may reveal. Derived from catalogue 05 row `p5d-readme-md` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/README.md` | `notes/README.md.bak` |
| `arc-readme` | `README` | — | exact | yes | source-code manifest | member_basename | Derived from catalogue 05 row `p5d-readme` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | medium | `project/README` | `notes/README.bak` |
| `arc-readme-txt` | `README.txt` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5d-readme-txt` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/README.txt` | `notes/README.txt.bak` |
| `arc-readme-rst` | `README.rst` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5d-readme-rst` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/README.rst` | `notes/README.rst.bak` |
| `arc-readme-adoc` | `README.adoc` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5d-readme-adoc` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/README.adoc` | `notes/README.adoc.bak` |
| `arc-readme-md-2` | `readme.md` | — | exact | no | source-code manifest | member_basename | Derived from catalogue 05 row `p5d-readme-md-2` — the two files share one source so they cannot drift. Re-kinded to `source-code manifest` because P5's `MARKER_KINDS` offers exactly two classes for archives. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/readme.md` | `notes/readme.md.bak` |
| `arc-doc-transcript` | `transcript \| transcripts` | `(?<![a-z])(?:transcript\|transcripts)(?![a-z])` | regex | no | document name | member_basename (extension removed), matched as a whole word | Named literally by §2.5's `submission.zip` example. Matched against the member's basename with separators normalised, because real archives spell it `Personal_Statement.pdf`, `personal-statement-final.docx` and `PersonalStatement.pdf`. **This is a name match, not a content claim:** the marker records that a member is *called* this, and whether the archive is an application packet is a purpose fact (§3.9) and P6's. | §2.5 "A ZIP file named `submission.zip` may contain a transcript, personal statement, resume, certificate, and form, which is meaningful evidence of a purpose-defined application packet even when the outer archive name is vague." | low | `submission/Transcript.pdf` | `submission/readme.txt` |
| `arc-doc-personal-statement` | `personal statement \| personal-statement \| personalstatement \| statement of purpose` | `(?<![a-z])(?:personal[ _-]?statement\|personal-statement\|personalstatement\|statement[ _-]?of[ _-]?purpose)(?![a-z])` | regex | no | document name | member_basename (extension removed), matched as a whole word | Named literally by §2.5. `statement of purpose` is included as the same document under its other standard name — the only synonym admitted anywhere in this array, and it is admitted because it is the identical artefact, not a related one. Matched against the member's basename with separators normalised, because real archives spell it `Personal_Statement.pdf`, `personal-statement-final.docx` and `PersonalStatement.pdf`. **This is a name match, not a content claim:** the marker records that a member is *called* this, and whether the archive is an application packet is a purpose fact (§3.9) and P6's. | §2.5 "A ZIP file named `submission.zip` may contain a transcript, personal statement, resume, certificate, and form, which is meaningful evidence of a purpose-defined application packet even when the outer archive name is vague." | low | `submission/Personal_Statement_Final.pdf` | `submission/readme.txt` |
| `arc-doc-resume` | `resume \| résumé \| cv \| curriculum vitae` | `(?<![a-z])(?:resume\|résumé\|cv\|curriculum[ _-]?vitae)(?![a-z])` | regex | no | document name | member_basename (extension removed), matched as a whole word | Named literally by §2.5. `CV` and `curriculum vitae` are the same document; `cv` is matched only as a whole word so it cannot fire inside `cvs`, `cvx` or a filename fragment. Matched against the member's basename with separators normalised, because real archives spell it `Personal_Statement.pdf`, `personal-statement-final.docx` and `PersonalStatement.pdf`. **This is a name match, not a content claim:** the marker records that a member is *called* this, and whether the archive is an application packet is a purpose fact (§3.9) and P6's. | §2.5 "A ZIP file named `submission.zip` may contain a transcript, personal statement, resume, certificate, and form, which is meaningful evidence of a purpose-defined application packet even when the outer archive name is vague." | low | `submission/Resume.pdf` | `submission/readme.txt` |
| `arc-doc-certificate` | `certificate \| certification` | `(?<![a-z])(?:certificate\|certification)(?![a-z])` | regex | no | document name | member_basename (extension removed), matched as a whole word | Named literally by §2.5. Matched against the member's basename with separators normalised, because real archives spell it `Personal_Statement.pdf`, `personal-statement-final.docx` and `PersonalStatement.pdf`. **This is a name match, not a content claim:** the marker records that a member is *called* this, and whether the archive is an application packet is a purpose fact (§3.9) and P6's. | §2.5 "A ZIP file named `submission.zip` may contain a transcript, personal statement, resume, certificate, and form, which is meaningful evidence of a purpose-defined application packet even when the outer archive name is vague." | medium | `submission/Certificate.pdf` | `submission/readme.txt` |
| `arc-doc-form` | `form` | `(?<![a-z])(?:form)(?![a-z])` | regex | no | document name | member_basename (extension removed), matched as a whole word | Named literally by §2.5. Highest false-positive risk in this array by a wide margin — `form` appears in `format`, `formula`, `information`, `transformation` — so it is matched as a whole word only, and even then it is one weak marker among several. §2.5's own example depends on **five** documents co-occurring, not on any one of them. Matched against the member's basename with separators normalised, because real archives spell it `Personal_Statement.pdf`, `personal-statement-final.docx` and `PersonalStatement.pdf`. **This is a name match, not a content claim:** the marker records that a member is *called* this, and whether the archive is an application packet is a purpose fact (§3.9) and P6's. | §2.5 "A ZIP file named `submission.zip` may contain a transcript, personal statement, resume, certificate, and form, which is meaningful evidence of a purpose-defined application packet even when the outer archive name is vague." | high | `submission/Form.pdf` | `submission/transformation-notes.pdf` |

## Refused — deliberately NOT matched

4 entries.

| id | match | match_kind | case sensitive | kind | applies to | rationale | design cite | FP risk | example true | example false (must NOT match) |
|---|---|---|---|---|---|---|---|---|---|---|
| `ref-office-internals` | `word/document.xml, xl/workbook.xml, ppt/presentation.xml, [Content_Types].xml` | exact | yes | source-code manifest | member_path_segment | These would let E4 recognise that a `.docx` opened as a ZIP is really a Word document. That is real information and it belongs somewhere else: §2.9 assigns routing to R, "inspect the real MIME type or file signature where possible, and dispatch each file to a type-specific extractor", and P5 SPEC Done-means 10 requires "routing follows signature over extension". Putting it here would put the routing decision in the marker recogniser, in a second place, where it would drift from R's. | §2.9 "treat the file extension as a routing signal rather than an assumption about meaning, inspect the real MIME type or file signature"; P5 SPEC Done-means 10 | n/a | `—` | `word/document.xml` |
| `ref-bomb-signals` | `high compression ratio, nested archive depth, declared uncompressed size` | exact | no | source-code manifest | n/a | Not markers. §2.5 makes these safety inputs — "Uncompressed size is read from the manifest where the format declares it, and is itself a bomb signal — it is never established by decompressing" — and E4 already consumes them to produce `unreadable` or `partial`. A marker is evidence about content; a bomb signal is a refusal to look. Keeping them apart is what stops a safety limit from becoming a fact about the file. | §2.5 "Password-protected, malformed, nested, or oversized archives should be marked as unreadable or partially inspected rather than forced open" | n/a | `—` | `—` |
| `ref-archive-name` | `the archive's own filename` | exact | no | document name | n/a | §2.5's example turns on the outer name being *vague*: `submission.zip` is "meaningful evidence of a purpose-defined application packet **even when the outer archive name is vague**". The members carry the evidence. The archive's own filename is already an observation at `zone = filename` from the filesystem run, and re-reading it here would double-count it. | §2.5 "A ZIP file named `submission.zip` may contain a transcript, personal statement, resume, certificate, and form, which is meaningful evidence of a purpose-defined application packet even when the outer archive name is vague." | n/a | `—` | `submission.zip` |
| `ref-document-type-vocabulary` | `cover letter, diploma, essay, invoice, contract, W-2, syllabus, thesis, …` | regex | no | document name | member_basename | The obvious extension of the document-name array, and refused. A general document-type vocabulary **is** the 200-300 template library (§5.7), which is deferred and owned by P10, and the residual library beyond §7.3's nine names is deferred too. Seeding it here — inside an archive marker list, where nobody would look for it — is exactly how a template library gets invented by accident and then diverges from the real one. §2.5 names five; this file authors five. | §5.7 template library — deferred to P10; P6 SPEC Deferred "Residual library contents beyond the nine §7.3 names … **P10**" | would be high | `—` | `submission/cover_letter.pdf` |

## Uncertain — needs Joseph

6 entries.

| id | match | match_kind | case sensitive | kind | applies to | rationale | design cite | FP risk | example true | example false (must NOT match) |
|---|---|---|---|---|---|---|---|---|---|---|
| `unc-form-word` | `form` | regex | no | document name | member_basename | §2.5 names `form` literally, so it is in — but it is the weakest string in either array. Whole-word matching keeps it out of `format`, `formula`, `information` and `transformation`, and the check asserts that. It will still fire on `Feedback Form.pdf`, `Form W-9.pdf` and `Google Form export.csv`, none of which makes an archive an application packet. Recommended handling, for P6 rather than for this file: weight `form` at zero on its own and let it count only alongside another document-name marker, which is what §2.5's five-document example actually describes. | §2.5 "A ZIP file named `submission.zip` may contain a transcript, personal statement, resume, certificate, and form, which is meaningful evidence of a purpose-defined application packet even when the outer archive name is vague." | high | `submission/Form.pdf` | `submission/transformation-notes.pdf` |
| `unc-cv-token` | `cv` | regex | no | document name | member_basename | Two characters, matched as a whole word. Safe against `cvs`, `cvx` and `opencv` because of the word boundaries, but a member literally named `cv.csv` in a data folder would match. Kept because `CV.pdf` is overwhelmingly the more common real case in the corpus this product targets. Flagged so it can be dropped cheaply if it proves noisy. | §2.5 "A ZIP file named `submission.zip` may contain a transcript, personal statement, resume, certificate, and form, which is meaningful evidence of a purpose-defined application packet even when the outer archive name is vague." | medium | `submission/CV.pdf` | `data/opencv-notes.txt` |
| `unc-nested-archive-members` | `markers inside a nested archive` | exact | no | source-code manifest | member_path_segment | An archive containing `project.zip` cannot have its inner members inspected without opening it, and §2.5 marks nested archives as a case to leave `unreadable` or `partial` rather than force open. So a nested archive's markers are simply not available, and the outer archive gets fewer markers than its contents would justify. This is a recall limit, not a bug, and it is the correct trade — but it should be visible before someone reads a sparse marker set as evidence of absence. | §2.5 "Password-protected, malformed, nested, or oversized archives should be marked as unreadable or partially inspected rather than forced open" | n/a | `outer/project.zip` | `—` |
| `unc-path-depth` | `should a marker at any depth count equally?` | exact | no | source-code manifest | member_path_segment | `package.json` at `project/package.json` is a project root marker. The same name at `project/node_modules/left-pad/package.json` is a dependency's manifest and means almost nothing — and a real Node archive contains thousands of them. P4 D10 already collapses repeated identical raw values within a zone, but these are *different* member paths, so they are different raw values and would not collapse. **A depth or path-segment filter is probably needed** — the natural rule being to ignore members under a `node_modules`, `vendor`, `site-packages` or `.venv` segment, reusing §1.1's eleven literal names in a different role. Not adopted unilaterally because it changes observation counts, which P2's replay diffs compare. | §1.1's eleven literal directory names; P4 D10 occurrence collapsing; §8.5 replay comparability | high | `project/node_modules/left-pad/package.json` | `project/package.json` |
| `unc-python-package-layout` | `"Python package layout" as a shape rather than a file` | exact | no | source-code manifest | member_path_segment | §2.5 names "Python package layout" alongside three concrete files, and a layout is a *relationship* between paths — a directory containing `__init__.py` plus sibling modules — not a single member. This file approximates it with the `__init__.py` row, which is the layout's defining file. A recogniser that genuinely tested the shape would need to reason over the whole member list, which it is given, so this is implementable; it is left out of v1 because a relationship-shaped marker has no obvious `member_path` to put in `raw_value`, and P5 requires one. | §2.5 "A source-code archive may reveal a `README.md`, `package.json`, `src` directory, or Python package layout and can be recognized as a code project." | low | `project/pkg/__init__.py` | `—` |
| `unc-localized-document-names` | `non-English document names` | regex | no | document name | member_basename | `Lebenslauf`, `curriculum`, `expediente`, `relevé de notes` — the same five documents in other languages. Same position as catalogue 04's localized screenshot row: easy to add, impossible to choose correctly without knowing which languages the corpus contains. One question to Joseph settles it. | §2.5 "A ZIP file named `submission.zip` may contain a transcript, personal statement, resume, certificate, and form, which is meaningful evidence of a purpose-defined application packet even when the outer archive name is vague." | low | `bewerbung/Lebenslauf.pdf` | `submission/resume.pdf` |

## Sources

- [Catalogue 05 — Repository markers and package manifests](./05-repository-markers.md) — **[internal]** retrieved 2026-08-20 — The source-code-manifest array below is generated from catalogue 05's `p5_evidence_markers` by `checks/build07.py`. Add a manifest there, not here.
- [PKWARE — APPNOTE.TXT, the .ZIP File Format Specification](https://pkware.cachefly.net/webdocs/casestudies/APPNOTE.TXT) — **[SEARCH SUMMARY — not opened]** consulted 2026-08-20 — The central-directory structure that makes manifest-only inspection possible — member paths and uncompressed sizes are readable without decompressing any entry, which is what §2.5's prohibition relies on.
- [Python — zipfile.ZipFile.namelist / infolist](https://docs.python.org/3/library/zipfile.html) — **[SEARCH SUMMARY — not opened]** consulted 2026-08-20 — The stdlib surface a caller's `read_manifest` would use to produce member paths and declared sizes without extraction.

---

_Generated from the JSON beside this file by `render.py`. Do not hand-edit: edit the JSON and re-run._
